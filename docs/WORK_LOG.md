# WORK_LOG — Журнал выполненных работ

> Записи добавляются после **каждой** завершённой задачи (новые сверху).
> Формат: дата; задача; что конкретно сделано; какие файлы изменены; что проверено/протестировано; известные проблемы; что рекомендуется делать следующим.
> Незавершённое не помечается как выполненное.

---

## 2026-09-26 — XXIII. Natural Voice: запуск владельцем с Windows — deploy-modal.ps1, фикс путей .sh, same-origin на modal.run

- **Дата:** 2026-09-26
- **Задача:** у владельца Windows; PR #8 не мержить; дать пошаговую инструкцию первого
  реального запуска vd17-design через Modal с Windows-компьютера. Проверить, выполним ли
  существующий deploy из PowerShell; если нет — не заставлять ставить лишнюю среду, а дать
  Windows-вариант. Ничего не разрабатывать и не исследовать сверх необходимого.

### Проверки перед правками (факты)

- `deploy-modal.sh` — bash-скрипт, из PowerShell не выполняется (WSL/Git Bash = лишняя среда).
- **Найден баг в `deploy-modal.sh`:** `cd "$(dirname "$0")/../.."` попадает в `research/`, а не
  в корень репозитория → `modal run research/tts/…` получил бы несуществующий путь. Устранено:
  цель вычисляется от расположения скрипта (`APP=…/modal_app.py`, запуск из любой папки).
  Проверено stub-прогоном: `modal run`/`modal deploy` получают корректный абсолютный путь.
- `python -m modal` работает (проверено, client 1.5.5) → ps1 не зависит от PATH пользователя.
- На GitHub Pages сейчас СТАРЫЙ интерфейс (PR #8 не смержен): подписи «Self-hosted TTS-сервер»,
  «Проверить», «Последний запуск»; lastSource «self-hosted сервер» — индикатор настоящего
  синтеза есть и в старом UI. Демо-ответ «Сегодня: стоматолог…» не совпадает ни с одним
  опубликованным MP3 vd17 (в git только t01 из базового набора + x00–x20/n00–n07).
- Приёмочная фраза «Алексей, сегодня двадцать шестое сентября…» среди фраз-образцов отсутствует
  (проверено grep по phrases_vd17.json/phrases.json).

### Что конкретно сделано

- **`research/tts/runtime/deploy-modal.ps1`** — нативный PowerShell-эквивалент .sh (Windows
  PowerShell 5.1-совместимый, UTF-8 с BOM — иначе кириллица ломается). Сам: находит Python
  3.10+ (`python` или `py -3`, с защитой от Microsoft Store-заглушки; при отсутствии — русская
  инструкция по установке с python.org и выход) → Modal CLI через `pip --user` (вызовы строго
  `python -m modal`) → `modal setup` (подсказки про «Continue with GitHub», «карта не нужна;
  если требуют — остановитесь») → `modal run …::download_weights` → `modal deploy` → финальная
  памятка (какой URL копировать, как разбудить, как остановить). Аудит PowerShell-ловушек:
  функция переименована в `RunPy` (имя `Py` рекурсивно перехватывало бы вызов `py`), `$0`/`$30`
  в строках экранированы, `"$app::…"` собрано конкатенацией (не интерполяцией). pwsh в песочке
  недоступен (CDN GitHub заблокирован) — скрипт проверен статически; логика идентична
  протестированному .sh.
- **`research/tts/runtime/deploy-modal.sh`** — фикс путей (см. выше).
- **`prototype/js/tts/providers.js`** — no-base guard сужен до `*.github.io`: HTTPS-страница,
  отданная самим TTS-сервером (`https://tts--…modal.run/`), с пустым полем сервера работает
  через относительный `/api/tts/health` (same-origin, без CORS) — это чистая поверхность для
  приёмочного теста «Проверка произвольной фразой».
- **Документация:** runtime/README (Windows-вариант запуска + примечание same-origin),
  TTS_RESEARCH §18.2a, CHANGELOG XVIII, эта запись.

### Что проверено/протестировано

- `prototype/tests/tts-proto-check.js` — **48/48** (новый S11: same-origin на …modal.run —
  пустое поле → относительный health → «подключён»; S8 сохранён: github.io без адреса —
  честное «статический хостинг», без запроса).
- Регресс: `dryrun_server_qwen3.py` 19/19, `dryrun_modal_layout.py` 10/10,
  `stage1-proto-check.js` 140/140, `normalize.test.js` 26/26.
- `deploy-modal.sh` — полный stub-прогон (fake HOME + stub `modal`): команды получают
  правильные абсолютные пути.

### Известные проблемы / границы (честно)

- deploy-modal.ps1 не выполнен на настоящей Windows (в песочнице нет pwsh — CDN заблокирован);
  статическая проверка + логика, идентичная протестированному .sh. При ошибке владелец
  присылает вывод — правим.
- Живой синтез по-прежнему требует действия владельца (аккаунт Modal через GitHub, без карты).
  Ничего не симулировалось.

### Рекомендуется следующим

1. Владелец выполняет инструкцию (ZIP ветки → PowerShell → `deploy-modal.ps1` → URL → Aven).
2. Подтверждение живого vd17-design → после этого обсуждаем merge PR #8.

---

## 2026-09-26 — XXII. Natural Voice `vd17-design`: GPU-runtime Modal для GitHub Pages + UX настроек голоса

- **Дата:** 2026-09-26
- **Задача:** приоритет владельца один — Aven на сайте должен реально говорить голосом
  `qwen3/vd17-design` **произвольным** текстом (не mock/не готовые MP3/не System TTS; они — только
  fallback/demo). Новых исследований голосов, voice-lab, 3D/lip-sync — не делать. Минимальные
  расходы, ничего платного без подтверждения, HTTPS для GitHub Pages, без временных LAN-IP как
  штатного адреса, без секретов в репозитории/frontend. Код подготовить до получения GPU,
  протестировать доступные части, commit+push+PR (merge не делать).

### Что конкретно сделано

- **Выбор провайдера (§18.1):** **Modal** — Starter $0/мес ($30 кредитов ежемесячно, без карты,
  вход через GitHub), serverless L4 ≈ $0,80/ч только во время синтеза, простой $0, автоматический
  постоянный HTTPS endpoint. Альтернативы отклонены: свой GPU-ПК+туннель (нужен домен, сложнее),
  RunPod/HF Endpoints (платно сразу), ZeroGPU (квота ~3,5 мин/день + токен в frontend), Colab/Kaggle
  (не постоянный адрес).
- **`research/tts/runtime/modal_app.py`:** деплой `research/tts/server.py` **без изменений**
  (`@modal.web_server` 0.0.0.0:8000, GPU L4/`AVEN_MODAL_GPU`, `scaledown_window=1200`,
  `max_containers=1`, Volume для HF-весов, `download_weights()` на CPU). Раскладка образа
  повторяет репозиторий (`/root/research/tts` + `/root/prototype`) — endpoint сам раздаёт прототип.
- **`research/tts/runtime/deploy-modal.sh`:** одно действие владельца (CLI → `modal setup` через
  GitHub → веса в Volume → `modal deploy` → печать URL и следующих шагов). PEP 668 учтён (venv).
- **`prototype/js/tts/providers.js`:** двухстадийный health (3 с → 75 с с `onWaking`,
  «просыпается…») для холодного старта serverless; новое состояние `no-base` — на HTTPS-странице
  без адреса сервера сразу честная причина, без запроса.
- **`prototype/js/data.js`/`state.js`:** `natural.voice` по умолчанию `qwen3/vd17-design` + миграция
  пустого значения из старых localStorage.
- **`prototype/js/settings.js`:** поле «TTS-сервер Natural Voice» (placeholder `https://…modal.run`,
  убран `http://192.168.1.10:8080`); статус «подключён · … · qwen3 (cuda) · vd17-design доступен ·
  N мс» / «НЕ подключён — причина» + подсказка; кнопка «Проверить backend» = health + настоящий
  короткий синтез vd17-design; строка «Проверка произвольной фразой» (приёмочный тест).
- **Документация:** `research/tts/runtime/README.md` (два пути: Б — Modal для GitHub Pages,
  А — свой GPU-ПК; troubleshooting с cold start), `docs/TTS_RESEARCH.md` §18 (+шапка),
  `docs/CHANGELOG.md` (XVII), `prototype/README.md`.

### Что проверено/протестировано

- `prototype/tests/tts-proto-check.js` — **47/47** (новые S0–S10: дефолт и миграция vd17, статус с
  устройством движка, cold start (двухстадийный health + onWaking), no-base на «GitHub Pages»
  (reconfigure на https), deep-check кнопкой (POST synthesize «Проверка Natural Voice.» + Audio),
  произвольная фраза — приёмочный текст).
- `research/tts/tests/dryrun_modal_layout.py` — **новый, 10/10**: раскладка Modal-образа,
  server.py из неё поднимает qwen3/vd17-design (заглушка модели), health/synthesize/GET //CORS.
- Регресс: `dryrun_server_qwen3.py` 19/19, `stage1-proto-check.js` 140/140, `normalize.test.js` 26/26.

### Известные проблемы / границы (честно)

- **Живой синтез на GPU не показан** — требует одного действия владельца (аккаунт Modal через
  GitHub, без карты → `bash research/tts/runtime/deploy-modal.sh`). Агент не может создать аккаунт
  за владельца; в песочнице нет GPU (2 vCPU/3,8 ГБ) и HuggingFace заблокирован. Ничего не
  симулировалось.
- Cold start Modal после ~20 мин простоя: первая фраза ждёт загрузки модели (десятки секунд,
  фронтенд показывает «просыпается…»), следующие — секунды. `min_containers=1` (всегда тёплый) —
  платно, сознательно не включено.
- Endpoint публичный (без аутентификации): знающий URL может синтезировать в пределах кредитов
  владельца — для личного демо приемлемо, URL не публиковать.
- Т4 на Modal не подходит (Turing без bf16); default L4.

### Рекомендуется следующим

1. Владельцу: аккаунт Modal (GitHub, без карты) → `bash research/tts/runtime/deploy-modal.sh` →
   URL в Настройки → Голос → «Проверить backend» → «Проверка произвольной фразой».
2. После подтверждения живого синтеза: замер latency на L4 (наши цифры, а не опубликованные),
   при желании — `AVEN_TTS_QWEN3_FA2=1`/flash-attn в образе Modal.
3. Merge PR — решением владельца.

---

## 2026-09-26 — XXI. Natural Voice Female Aven — runtime: аудит HTTP 404, Qwen3-движок сервера, честный frontend

- **Дата:** 2026-09-26
- **Задача:** по приоритету владельца (только Natural Voice Runtime; события/accessibility/authorization/
  админка/Stage 1.0 — backlog; 3D не делать): разобраться, почему Natural Voice `vd17-design` фактически
  не работает («не подключён — HTTP 404»), и довести путь до реального end-to-end: Главная →
  «Что у меня сегодня?» → demo-ответ → Natural TTS → воспроизведение → «Говорю…» → «Готова», либо
  честный fallback на System TTS. Голос не менять, новый широкий TTS-research не запускать, 3D не трогать.

### Аудит (перед изменениями)

Установлено по коду: провайдеры — `SystemTTSProvider` и `NaturalTTSProviderExperimental` в
`prototype/js/tts/providers.js`; `speechSynthesis` вызывается только внутри System-провайдера;
engine — `settings.voice.engine`, голос — `settings.voice.natural.voice` (localStorage `aven-proto-v1`);
адрес `192.168.1.10` — **placeholder** поля ввода (`settings.js`), реальное значение вводил владелец
(в git его нет). Frontend ждёт: проверка `GET {base}/api/tts/voices`, синтез
`POST {base}/api/tts/synthesize {text, voice, rate}` → `audio/wav`. **Причина HTTP 404:** сервер по
адресу ОТВЕТИЛ, но пути нет → там работал **другой** сервер, не `research/tts/server.py` (наш на
`/api/tts/voices` всегда отвечал бы 200 JSON; mixed content дал бы `TypeError`, а не код 404) —
вероятно, Qwen3-TTS-подобный сервер с другими путями. **Второй структурный mismatch:** в самом
`server.py` Qwen3-движка не было (только RHVoice/Silero/Supertonic/Piper) — `vd17-design` на нём
появиться не мог.

### Что конкретно сделано

- **`research/tts/server.py` → v0.3.0.** Новый движок **Qwen3 VoiceDesign** (включается
  `AVEN_TTS_QWEN3=1`): модель `Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign`, голос `qwen3/vd17-design`,
  промпт — общий `common.DESIGN_PROMPT` (иначе тембр ≠ прослушанному), CUDA при наличии + громкое
  предупреждение на CPU, необязательная FlashAttention-2 (`AVEN_TTS_QWEN3_FA2=1`), модель kept-alive,
  флаг `--warmup` (прогрев одной короткой фразой). `GET /api/tts/health` расширен:
  `{ok, status, server:'aven-tts-research', version, engines{model,device,dtype,load_s,supports_rate},
  voices, uptime_s, max_chars}`; `/api/tts/voices` сохранён для совместимости. Честный заголовок
  `X-Rate-Applied: ignored`, когда движок скорость не применяет. **CORS — контролируемый (не `*`):**
  allowlist `https://nub36.github.io` + localhost/127.0.0.1 (любой порт) + `AVEN_TTS_ORIGINS`;
  preflight Private Network Access. Тексты запросов по-прежнему не логируются (IP, метод, путь,
  число символов, секунды).
- **Runtime для владельца — `research/tts/runtime/`:** `README.md` (требования GPU ≥8 ГБ VRAM,
  venv, torch CUDA, `AVEN_TTS_QWEN3=1 python research/tts/server.py --warmup`, curl-проверки,
  подключение прототипа, работа по LAN и через GitHub Pages, mixed content, HTTPS-варианты,
  troubleshooting-таблица, приватность) и `requirements.txt` (`qwen-tts==0.1.1` — версия CI-прогонов;
  torch ставится отдельно под CUDA/CPU). Веса (≈4,5 ГБ) качаются с Hugging Face владельцем,
  в git не попадают.
- **Frontend `prototype/js/tts/providers.js`:** health-check переключён на реальный endpoint
  (stare серверы без `voices` в health — добор через `/api/tts/voices`); классификация неудач:
  HTTP-код с подсказкой «по адресу НЕ Aven TTS server» (404), timeout (3 с), **mixed content**
  (https-страница → http-сервер определяется до запроса), сеть/CORS; latency замеряется.
  **Таймаут синтеза** `natural.timeoutSec` (по умолчанию 10 с; CPU-проверка собственного сервера —
  поднять до ~120) с разделением `NaturalTimeout` (→ fallback + тост) и отмены пользователем
  (→ тишина, без тоста). Natural-провайдер получил `health()/getStatus()/getVoice()` поверх
  `available()/speak()/stop()`. Тосты честности: «Natural Voice недоступен (причина) — используется
  системный голос» / «Natural Voice не ответил вовремя — используется системный голос».
  Presence-подписи источника: «Говорю · Natural» / «Говорю · системный голос» (`presence.js`
  принимает точный label).
- **`settings.js`:** статус сервера теперь «подключён · aven-tts-research 0.3.0 · qwen3 · голосов: N ·
  N мс» или конкретная причина; новое поле «Таймаут Natural, сек». **`data.js`:** дефолт
  `natural.timeoutSec: 10`; demo-regex ассистента дополнен `пробег` (фраза «Какой пробег?» из
  тест-плана владельца теперь попадает в автомобильный ответ).
- **НЕ делалось (по списку владельца):** 3D/lip-sync/avatar/Male, события, accessibility, authorization,
  админка, Stage 1.0, AI/LLM, production-backend, новый широкий TTS-research, новые ADR,
  streaming (сознательно отложен: сначала стабильный request→audio→playback), кэш-сервер на стороне
  TTS-сервера (есть только сессионный кэш вкладки).

### Какие файлы изменены

- Изменено: `research/tts/server.py`, `prototype/js/tts/providers.js`, `prototype/js/presence.js`,
  `prototype/js/settings.js`, `prototype/js/data.js`, `prototype/README.md`, `docs/VOICE.md`,
  `docs/TTS_RESEARCH.md`, `docs/CHANGELOG.md`, `docs/WORK_LOG.md` (эта запись)
- Создано: `research/tts/runtime/README.md`, `research/tts/runtime/requirements.txt`,
  `prototype/tests/tts-proto-check.js`, `research/tts/tests/dryrun_server_qwen3.py`

### Что проверено/протестировано

- **`prototype/tests/tts-proto-check.js` (новый, jsdom): 37/37.** S1–S6 статус сервера в Настройках
  (подключён/HTTP 404/сеть-CORS/timeout + поле таймаута); **A:** Главная «Что у меня сегодня?» →
  demo-ответ → POST голосом `vd17-design` с нормализованным текстом → speaking → idle, источник
  «self-hosted сервер», без тоста fallback; **B/C:** деньги и пробег словами («рублей», без цифр;
  «сто четыре тысячи пятьсот двадцать километров»), экранный текст с цифрами; **D/E:**
  «пятнадцать минут» неизменно; лейблы «Говорю · Natural»/«Готова»; **F:** сеть недоступна →
  System TTS тем же текстом + честный тост + «Говорю · системный голос»; **G:** 404 → fallback с
  `HTTP 404`; **H:** таймаут 3 с → fallback «не ответил вовремя» → «Готова»; **I:** stop прерывает
  воспроизведение → idle; **J:** новая речь останавливает предыдущую (pause), тоста-ошибки нет;
  **N:** нормализация контрактных примеров (3 420 ₽, 104 520 км, 10:30, Aven→Авен).
- **`research/tts/tests/dryrun_server_qwen3.py` (новый, настоящий ThreadingHTTPServer + заглушка
  qwen_tts): 19/19** — движок поднимается по env, health/voices/synthesize/400/404, RIFF/WAV,
  `X-Synth-Seconds`, `X-Rate-Applied: ignored`, CORS: nub36.github.io/localhost/чужой origin/без origin,
  OPTIONS+Private Network Access, warmup. В логе сервера — только `chars=`, текста запросов нет.
- **Регресс:** `stage1-proto-check.js` **140/140**, `normalize.test.js` **26/26**, `check.py` **0/0**,
  `node --check` всех изменённых JS, `py_compile` обоих Python-файлов.
- **Живой сервер поднят** (`python3 research/tts/server.py --host 0.0.0.0 --port 8080`, БЕЗ движков —
  честно): `curl /api/tts/health` → `{ok:true, status:'ok', server:'aven-tts-research', version:'0.3.0',
  engines:{}, voices:[]}`; synthesize → честный 400; CORS-preflight отражает только разрешённый origin;
  прототип отдаётся на `/`.

### Известные проблемы / честные ограничения

- **GPU runtime НЕ запускался.** В песочнице нет GPU (`nvidia-smi` отсутствует), Hugging Face
  заблокирован прокси, RAM 3 ГБ: Qwen3 здесь поднять невозможно. Реальный звук `vd17-design` появится
  только после запуска сервера на GPU-машине владельца по `research/tts/runtime/README.md`
  (≈15–20 минут + скачивание ≈4,5 ГБ). Это НЕ объявление «Natural Voice работает».
- **Где именно запущен чужой сервер 192.168.1.10:808x и что это за сервер** — установить из песочницы
  нельзя (LAN владельца). Вывод «другой сервер / другие пути» следует из самого факта HTTP 404 и
  контракта; новый статус в Настройках теперь прямо подсказывает эту причину.
- **Mixed-content guard** покрыт код-ревью, но не jsdom-проверкой (тестовая страница по http;
  ветка срабатывает только на https).
- **Public HTTPS demo не существует** — GitHub Pages статичен; для публичного Natural нужен HTTPS
  endpoint (reverse-proxy TLS / туннель) — решение и выбор способа за владельцем (TTS_RESEARCH §13).
- Latency GPU не измерялась нами (§12.2 — опубликованные данные Qwen, помечены как чужие);
  измерение на целевом железе — следующий шаг владельца/агента после запуска.

### Что рекомендуется делать следующим

1. **Владельцу:** на ПК с GPU выполнить `research/tts/runtime/README.md` → открыть прототип по
   `http://<IP-ПК>:8080/` → Настройки → Голос → Натуральный → `vd17-design` → «Проверить» → Главная →
   «Что у меня сегодня?». Если GPU нет — CPU-проверка возможна, но с таймаутом ~120 с и только как
   проверка связки (не для диалога: 18–70 с на фразу, TTS_RESEARCH §12.1).
2. **После запуска:** измерить реальную latency (TTFB/полное время/прогрев/VRAM) и записать в
   TTS_RESEARCH — до решений о streaming (vLLM-Omni/чанкинг, §12.5) и о public HTTPS demo.
3. **Public demo (если нужно):** выбрать HTTPS-способ (TLS reverse-proxy на своём ПК/туннель),
   добавить origin в `AVEN_TTS_ORIGINS`, указать HTTPS-адрес в настройках прототипа.
4. Следующий отдельный этап после одобрения владельца — **3D Female Bust / Face Animation / Lip-sync**
   (не начинать в этой задаче).

---

## 2026-09-26 — XX. Прототип: сквозная история и настоящий Undo во всех разделах, деньги в минимальных единицах, экспорт и удаление данных

- **Дата:** 2026-09-26
- **Задача:** продолжить работу по плану в прежних рамках (документация и прототип; production-код и spikes
  запрещены до перевода стековых ADR в `Accepted`). Выбрано: закрыть разрыв по **сквозным требованиям** среза
  1.0 — [MVP_SCOPE.md](MVP_SCOPE.md) §5.9 и §6.2 (история и Undo для всех изменений), §5.6 (деньги целыми в
  минимальных единицах, удаление операции, экспорт CSV), §6.5 (экспорт и удаление данных — «в 1.0,
  не «когда-нибудь»), §5.1 приёмка 4 и §7 (подтверждения и повторная аутентификация).
  Причина выбора: после записи XIX история писалась только из админки, настроек и экранов аккаунта, а Undo
  лишь помечал запись — то есть критерии «каждое изменение данных оставляет запись» и «Undo возвращает
  прежнее состояние» (§5.9, приёмка 1–2) для пользовательских разделов не выполнялись.

### Что конкретно сделано

- **Механизм настоящего Undo (`prototype/js/history.js`).** Запись истории получила данные для отмены
  (JSON-сериализуемые, живут в той же записи): `restore` (вернуть удалённый объект на прежнее место),
  `remove` (убрать созданный), `fields` (вернуть прежние значения полей), `value` (вернуть значение вне списка,
  например пробег авто) и `adjust` (поправить числовые итоги, например `finMonth.expense`). Списки могут быть
  вложенными (`car.fuel`). `A.undoAction()` применяет отмену к данным, помечает запись и пишет обратную запись
  в историю; если данных для возврата нет (демо-записи `data.js`), прототип говорит прямо —
  «отменено (только запись)» — вместо имитации возврата (ADR-010).
- **Общие помощники для всех разделов:** `A.downloadFile()` (реальная выгрузка файла через Blob, честный отказ,
  если окружение не умеет), `A.minor()`/`A.sumMoney()` (деньги целыми в минимальных единицах), `A.indexOfId()`
  (прежнее место объекта для Undo).
- **Сквозная история в разделах (`js/pages1.js`, `js/pages2.js`):** задачи (создание, отметка «выполнено»,
  удаление), заметки (создание, редактирование — в историю попадают только реально изменённые поля,
  закрепление, удаление), финансы (создание операции, удаление), а также разделы 1.1 и Stage 4 — заправки,
  расходы и обслуживание авто, пробег, покупки, включение/выключение и создание автоматизаций. У каждой
  записи — старое/новое значение, метки `danger`/`sensitive` и данные для отмены.
- **Финансы по §5.6:** суммы и итоги считаются целыми копейками (`A.sumMoney`), баланс пересчитывается при
  добавлении и удалении операции, удаление требует подтверждения и отменяется, добавлен экспорт операций в
  CSV (с подтверждением как операция с приватными данными, §7), на странице показана проверка точности
  `0.1 + 0.2 = 0,30 ₽` (в копейках `10 + 20 = 30`) против float-артефакта, отрицательный баланс показывается
  с пометкой, а не запрещается.
- **Приватность по §6.5 (`js/settings.js`):** «Экспорт всех данных» — JSON со всеми записями аккаунта
  (подтверждение обязательно), ссылки на экспорт истории (JSON) и операций (CSV), «Удаление аккаунта и всех
  данных» — повторная аутентификация + ввод слова `DELETE`; после удаления данные очищаются, сессии
  завершаются, пользователь выходит, а след остаётся в административном аудите, а не в пользовательской
  истории (ADR-012). Экран входа честно сообщает об удалении и предлагает вернуть демо-данные.
  «Сброс демо-данных» оставлен отдельной строкой — это не удаление аккаунта.
- **Повторная аутентификация по §7 и §5.1 приёмка 4:** «подтвердите паролем сейчас» для отключения 2FA,
  смены пароля и удаления аккаунта (демо-пароль указан в форме открыто — настоящей проверки пароля в
  прототипе нет и быть не может). Смена пароля валидирует длину (≥8) и совпадение, значение пароля в историю
  не попадает. При ошибке действие не выполняется.
- **Убрана имитация:** форма «Новое событие» сообщала «сохранено», хотя ничего не сохраняла — теперь честно
  пишет, что раздел событий в прототипе ещё на статичных данных (MVP_SCOPE §5.4); реализация событий —
  следующая задача.

### Найденные и исправленные дефекты прототипа

1. **Пустой экспорт данных:** `export-state` в настройках вызывал `JSON.stringify(S.s, null, 2)` —
   сериализовалась **функция** `S.s`, а не состояние `S.s()`, поэтому скачанный файл был пустым.
   Исправлено, выгрузка переведена на общий `A.downloadFile` и подтверждение (§7).
2. **Двойное срабатывание чекбоксов:** в браузере клик по `<label>` с чекбоксом порождает два `click`-события
   (по метке и синтетическое по чекбоксу), из-за чего `toggle-task` выполнялся дважды: задача отмечалась и тут
   же размечалась, а в историю попадали две записи. Делегирование в `js/app.js` перестроено: чекбоксы и
   радиокнопки обрабатываются только событием `change` (одно на взаимодействие, работает и с клавиатуры —
   §6.4), `click` их пропускает. Проверено: один клик по подписи = ровно одна запись.
3. **Рассинхрон переключателя 2FA:** при отказе в повторной аутентификации переключатель оставался в положении
   клика — добавлен возврат к сохранённому значению через `A.render()`.

### Что проверено

- `NODE_PATH=/tmp/lab/node_modules node prototype/tests/stage1-proto-check.js` — **140 проверок jsdom,
  0 провалов**. Новые секции: **G** (33 проверки) — создание/отметка/удаление задачи с Undo и возвратом на
  прежнее место, один клик по метке = одно действие, заметки (создание/закрепление/редактирование с записью
  только изменённых полей/удаление + Undo каждого шага), финансы (итоги в копейках `+30` мин. ед. за `0.1` и
  `0.2`, пересчёт баланса, удаление с подтверждением, Undo с обратным пересчётом, `0.1 + 0.2 = 0.3`,
  подтверждение и честный результат экспорта CSV), авто/покупки/автоматизации (включая Undo значения вне
  списка); **H** (20 проверок) — экспорт всех данных (перехват содержимого файла: реальное состояние, а не
  `undefined`), удаление аккаунта (отказ без слова `DELETE`, отказ при неверном пароле, успешное удаление,
  след в аудите при пустой пользовательской истории, возврат демо-данных), 2FA (отключение только через
  пароль, возврат переключателя при отказе, включение без пароля), смена пароля (re-auth, длина, совпадение,
  необратимая запись без значения пароля).
- Регрессия прежних 119 проверок (меню, история, админка, тема, экраны аккаунта, роут-гард, 12 страниц,
  13 разделов настроек) — без провалов; `node --check` для всех изменённых JS; `research/tts/check.py` — 0/0.
- Документы: [MVP_SCOPE.md](MVP_SCOPE.md) §5.10 (блок «Обновление 2026-09-26, вторая итерация»), §6.5, §7;
  [PROJECT_PLAN.md](PROJECT_PLAN.md) вопросы №19 и №21 дополнены тем, что теперь видно в прототипе
  (решения владельца не подменены); `prototype/README.md`.

### Известные ограничения (честность)

- Повторная аутентификация в прототипе принимает демо-пароль `demo-pass-123` и он написан в форме открыто:
  настоящего хэширования и проверки нет (появятся после `Accepted` ADR и ADR-110).
- События и календарь (§5.4) остаются статичными: форма честно говорит, что ничего не сохраняет. Реальное
  создание с повторениями и пересечением по времени — следующая задача.
- Экспорт файлов в jsdom недоступен (нет `URL.createObjectURL`), поэтому в тестах успех проверяется
  с подменой Blob/URL; в браузере скачивание настоящее.
- Состояния «нет прав» и «загрузка/ошибка» (§5.3–§5.6), полный проход клавиатурой и фокус-ловушка модальных
  окон (§6.4) в прототипе ещё не моделируются.

### Что рекомендуется следующим

1. **§5.4 события:** реальное создание/изменение/удаление записей, показ пересечений по времени
   («показать, не запрещать»), связь с «Днём» и «Главной», Undo.
2. **§6.4 доступность:** полный проход клавиатурой, видимый фокус, фокус-ловушка и Esc в модальных окнах,
   aria-атрибуты и метки полей, озвучивание изменений экрана.
3. **§6.1 authorization на объекте:** демо-роли и состояние «нет прав» (кто может отменить чужое действие).
4. **Ответы владельца** на [MVP_SCOPE.md](MVP_SCOPE.md) §12 и STACK_RESEARCH §13 — без них состав 1.0 и стек
   остаются черновиком; перевод ADR-101/103/113 в `Accepted` открывает spikes и реализацию.

---

## 2026-09-26 — XIX. Прототип: экраны аккаунта, история действий с Undo, минимум админки, тема «как в системе»

- **Дата:** 2026-09-26
- **Задача:** продолжить работу по плану в разрешённых рамках (ответ владельца 2026-09-26: направление выбирает
  агент, но только документация и прототип; production-код и spikes запрещены до перевода стековых ADR в
  `Accepted`). Выбрано: **устранить разрыв между срезом Stage 1.0 и прототипом** — из 12 пунктов среза
  ([MVP_SCOPE.md](MVP_SCOPE.md) §4.1) три не были представлены совсем (п.1 аккаунт, п.9 история + Undo,
  п.11 минимум админки), плюс открытые вопросы №32 (тема), №34 (адаптивность), №35 (навигация профиля).
  Причина выбора: п.9 — сквозной слой, который по §3 нужно проектировать с первой записи, а п.1 и п.11
  определяют роут-гард и разделение `/settings` и `/admin` ([ADR-012](DECISIONS.md)); без них прототип не даёт
  материала для ответов владельца на §12.

### Что конкретно сделано

- **Экраны аккаунта — новый файл `prototype/js/auth.js`** (`#/login`, `#/register`, `#/recovery`): вход с
  валидацией полей (формат email, пароль ≥8 символов), шаг 2FA/TOTP после пароля, регистрация (имя, email,
  пароль, подтверждение, обязательная отметка «это прототип»), восстановление доступа, включение/выключение
  2FA, завершение сессий, выход. Экраны аккаунта рисуются без сайдбара и топбара (`body.auth-mode`).
  Роут-гард в `app.js`: при `auth.logged === false` любой раздел возвращает на `#/login`, при `true` —
  обратный переход на `#/home`.
- **История действий и Undo — новый файл `prototype/js/history.js`** (`#/history`): записи с типом, объектом,
  временем, источником и метками (отменяемое / опасное / чувствительное), фильтры (тип, отменённые, поиск),
  статистика, подробности со сравнением «было → стало», экспорт JSON (реальная выгрузка файла через Blob) и
  **Undo** с обратными изменениями и собственной записью об отмене. Добавлены сквозные помощники
  `A.logAction(e)`, `A.nowLabel()`, `A.undoAction(id)` — их используют админка, настройки и экраны аккаунта,
  поэтому история действительно сквозной слой, а не отдельная страница.
- **Минимум админки — новый файл `prototype/js/admin.js`** (`#/admin`, 8 разделов): пользователи (роли,
  блокировка), роли и права, аудит, миграции, health, бэкапы (восстановление требует ввода слова `RESTORE`),
  feature flags, аварийные переключатели. Каждое изменение пишет запись в аудит и в историю; опасные действия
  проходят через подтверждение с явным текстом о необратимости; закрытие регистрации в аварийных
  переключателях реально закрывает `#/register` (`admin.emergency.registrationsClosed` ⇔ `auth.registrationsOpen`).
- **`prototype/js/app.js`:** пункты меню «История» и группа «Система» с «Админкой»; заголовки новых страниц;
  `A.resolvedTheme()` и тема «как в системе» (`prefers-color-scheme`: немедленное применение и реакция на смену
  системной темы); **исправлена ошибка** — нижняя группа меню (Настройки, Профиль) рисовалась дважды; у
  разделов меню вне среза 1.0 появилась метка этапа (`stage-badge`).
- **`prototype/js/settings.js`:** третий вариант темы («как в системе»); раздел «Безопасность» приведён к
  составу 1.0 (переключатель 2FA, резервные коды, «завершить другие сессии», завершение конкретной сессии,
  смена пароля с подтверждением, ссылка на журнал безопасности, политика 2FA по ролям помечена открытым
  вопросом SECURITY §11.3, кнопка «Выйти и показать экран входа»); «Приватность» ведёт в историю действий и её
  экспорт; у всех разделов вне среза 1.0 — метка этапа и предупреждение при открытии (не входит в 1.0,
  заглушки запрещены — [ADR-010](DECISIONS.md)).
- **`prototype/js/data.js`:** демо-данные `auth` (`twoFactor`, `registrationsOpen`, `recoveryNote`), `history`
  (8 записей: 6 отменяемых, 1 опасная, 1 чувствительная) и `admin` (3 пользователя, 3 роли, 5 записей аудита,
  3 миграции, health, 3 бэкапа, 5 флагов, аварийные переключатели); сессии получили `id`.
- **`prototype/index.html` и `css/style.css`:** кнопка «Выйти» в топбаре; подключение трёх новых скриптов в
  правильном порядке (history → admin → auth → app); стили экранов аккаунта, истории, админки и меток этапов;
  адаптивность новых экранов (≤860px — одна колонка, прокрутка таблиц, действия истории в строку);
  селектор инпутов расширен на `email`/`tel`/`url`.
- **Проверки — новый файл `prototype/tests/stage1-proto-check.js`:** 86 проверок jsdom; скрипт сам поднимает
  статический сервер, в репозитории намеренно нет `package.json`/`node_modules` (инструкция в шапке файла).
- **Документы:** [MVP_SCOPE.md](MVP_SCOPE.md) §4.1 (отметки «есть» у п.1, 9, 11 + блок «Обновление 2026-09-26»),
  §8 (метки этапов, тема, раздел «Безопасность»), §12 вопрос 12; [PROJECT_PLAN.md](PROJECT_PLAN.md) — вопросы
  №32, 34, 35 дополнены фактами о прототипе (решения владельца по-прежнему не приняты и не подменены);
  [ADMIN.md](ADMIN.md) и [SECURITY.md](SECURITY.md) — указатели на UX-макеты; `prototype/README.md` — описание
  новых экранов, их ограничений и способа проверки.

### Что проверено

- `NODE_PATH=/tmp/lab/node_modules node prototype/tests/stage1-proto-check.js` — **86 проверок, 0 провалов**:
  меню и метки этапов; история (8 записей, Undo с обратной записью и изменениями, фильтры, честное пустое
  состояние, подробности, экспорт без падения); админка (все 8 разделов, подтверждение опасного действия,
  блокировка пользователя с записью в аудит, применение миграции, отказ при неверном слове `RESTORE` и успех
  при верном, флаг с меткой этапа, связь аварийного переключателя с `#/register`); тема (три варианта;
  «как в системе» → светлая, когда `prefers-color-scheme: dark` не совпал); настройки (метки этапов,
  предупреждение у раздела вне 1.0, 2FA, сессии, пароль, ссылка на историю); экраны аккаунта (выход,
  роут-гард, валидация, шаг 2FA, отказ короткого кода, успешный вход, регистрация с ошибками и с успехом,
  восстановление без имитации письма); регрессия 12 прежних страниц и 13 разделов настроек.
- `node --check` для всех изменённых и новых JS-файлов; `python3 research/tts/check.py` — без изменений.
- Дефекты, найденные проверками и исправленные: дублирование нижней группы меню; **реальная ошибка `auth.js`** —
  примечание предыдущей попытки блокировало следующую попытку регистрации (`note` очищается, ошибка чекбокса
  выводится рядом с чекбоксом); рассинхрон переключателей админки после модального подтверждения (вызов
  `A.render()`); в самом тесте — устаревшая ссылка на `select` темы после перерисовки.

### Известные ограничения (честность, ADR-010)

- Это **прототип**: нет backend, БД, хэширования паролей и настоящего TOTP — 2FA принимает любой 6-значный код;
  email не отправляется (провайдер не выбран, вопрос №16), поэтому экран восстановления сообщает об ограничении
  вместо имитации «письмо отправлено».
- Разделы вне среза 1.0 не спрятаны, а помечены меткой этапа и предупреждением: прототип проектирует весь UI,
  но не делает вид, что всё это войдёт в первый релиз.
- Роль и права в демо статичны (`role: owner`): проверка прав на объекте (MVP_SCOPE §6) и экран «Нет прав»
  в прототипе не моделируются.

### Что рекомендуется следующим

1. **Ответы владельца** на чек-лист [MVP_SCOPE.md](MVP_SCOPE.md) §12 (16 вопросов) и STACK_RESEARCH §13 —
   без них состав 1.0 и стек остаются черновиком; обновлённый прототип даёт наглядный материал по вопросам
   12 (тема), 13 (адаптивность), 14 (навигация) и по истории/Undo и минимуму админки.
2. После перевода ADR-101/103/113 в `Accepted` — spikes S1–S5 и первая реализация (сейчас по-прежнему запрещены).
3. Возможные доработки прототипа: модель прав на объекте (кто может отменить чужое действие), экран «Нет прав»,
   пустые состояния истории и админки для нового аккаунта.

---

## 2026-09-26 — XVIII. Состав Stage 1 / MVP: пакет решений + зафиксированы ответы владельца

- **Дата:** 2026-09-26
- **Задача:** продолжить работу после ответов владельца на чек-лист стека. Владелец решил: развилка **R1**
  (где живёт backend) — **отложена**, развилка **R2** (где хостим) — **отложена**, проверки стека
  (spikes S1–S5) — **не разрешены до перевода ADR в `Accepted`**, следующее направление агент выбирает сам
  (в рамках ограничений: документация и прототип, production-код запрещён — AGENT_GUIDE §8).
  Выбрано: **продуктовый пакет решений по составу Stage 1 / MVP** (открытые вопросы №8 и №31), потому что
  даже утверждённый стек не даёт старта без ответа «какие разделы входят в первый срез».

### Что конкретно сделано

- **Ответы владельца зафиксированы в документах** (решение владельца — приоритетный источник требований,
  AGENT_GUIDE §4): STACK_RESEARCH §12 (блок «Ответ владельца 2026-09-26: spikes НЕ разрешены», R1/R2 отложены)
  и §13 (пометки у вопросов 1, 2, 14, 16); DECISIONS — шапка секции `Proposed` (статусы ADR-101/103/113
  остаются `Proposed`); PROJECT_PLAN — вопрос **№39 закрыт** (spikes не разрешены до `Accepted`, план сохранён)
  и добавлен вопрос **№40** (R1/R2 отложены владельцем).
- **Новый документ `docs/MVP_SCOPE.md`** (422 строки) — пакет решений по составу Stage 1:
  - **§2** восемь критериев отбора разделов (К1–К8), каждый со ссылкой на источник в наших документах
    (работает без голоса и команд; без платного AI; без Command Engine/автоматизаций/мобильных; полезен при
    пустых данных; контроль данных пользователем; история и Undo; не добавляет инфраструктуры; есть прототип).
  - **§3** зависимости данных и два вывода: агрегаторы («Главная», «День») имеют смысл только после 2–3
    источников данных; история/Undo — **сквозной слой с первой записи**, иначе переделывать все разделы.
    Сформулирована минимальная «петля ценности»: записал → увидел → изменил → отменил → в истории.
  - **§4** предлагаемый срез: **Stage 1.0** — 12 пунктов (аккаунт с сессиями и 2FA/TOTP, профиль, задачи,
    события/календарь, заметки, базовые финансы, «День», «Главная», история + Undo, настройки, минимум
    админки, 6 локальных инструментов) с обоснованием и отметкой «есть ли в прототипе»; **порядок работ
    внутри 1.0** и естественная точка альфа-проверки после финансов; **Stage 1.1** (авто, покупки/имущество,
    напоминания и уведомления, графики финансов, suggestions на правилах, утро/вечер, простые правила, файлы)
    с причиной отложения каждого; **явно не входит** (команды, голос, Canvas, мобильные/desktop/offline, AI,
    3D-персонаж, персонаж по умолчанию, банковские интеграции, валюты, EXIF, пользовательские сущности,
    совместный доступ) + правило честности: отсутствующих разделов в UI нет, заглушки запрещены (ADR-010).
  - **§5** спецификации девяти областей 1.0: поля и валидация, экраны, состояния (пусто/загрузка/ошибка/
    нет прав/просрочено/пересечение/отрицательный баланс), 3–5 критериев приёмки на область — включая
    требование «мышью **и** только клавиатурой» и проверяемую точность денег (0.1 + 0.2 = 0.3).
  - **§6–7** сквозные требования (authorization на объекте, история/Undo, честность статусов, доступность,
    экспорт и удаление данных, целые деньги, ru/en-разделение, без внешних сервисов, feature flags) и
    **перечень опасных действий 1.0** + предложение перечня чувствительных настроек (вопрос №19) и простого
    механизма re-auth «подтвердите паролем сейчас» (вопрос №21).
  - **§8** минимальный состав настроек 1.0 (7 разделов входят; 8 — нет, и они честно не показываются).
  - **§9** инструменты первого релиза: 6 локальных (даты/интервалы, проценты, единицы, пароли, QR, текстовые
    утилиты) и что отложено (топливо/поездка — к «Авто», валюты — провайдер не выбран, кредит/вклад, EXIF);
    честно отмечено, что в прототипе 13 карточек инструментов и часть из них — макеты.
  - **§10** дефолт главной (5 карточек; конструктор и пользовательские показатели отложены) и три варианта
    строки «Что сделать?» с оценкой: вариант «как в прототипе» (передача в Assistant) в 1.0 был бы **имитацией**
    функции, так как Command Engine появляется в Stage 2.
  - **§11** восемь проверяемых критериев «сайт действительно полезный и самостоятельный» (включая ≤10 минут
    на первые три записи без подсказок, работу только с клавиатуры, экспорт/удаление данных, отсутствие
    внешних API и платного AI, восстановимый бэкап).
  - **§12** чек-лист из **16 вопросов владельцу**; **§13** риски (scope creep, агрегаторы раньше данных,
    история «потом», разрастание админки, неутверждённый стек, файлы и email вне 1.0, отсутствие оценок);
    **§14** ограничения честности.
- **PROJECT_PLAN.md:** черновик MVP (раздел 5) получил ссылку на пакет решений и пояснение, что список
  остаётся черновиком до ответа владельца; указатели на предложения добавлены в открытые вопросы
  **№8, 9, 19, 21, 23, 27, 28, 29, 30, 31, 32, 33, 34, 35**; `MVP_SCOPE.md` внесён в карту документации (§3).
- **README.md** — новый документ в таблице «Где что находится»; **CHANGELOG.md (XIII)** — запись.

### Какие файлы изменены

- Создано: `docs/MVP_SCOPE.md`
- Обновлено: `docs/PROJECT_PLAN.md`, `docs/DECISIONS.md`, `docs/STACK_RESEARCH.md`, `docs/CHANGELOG.md`,
  `docs/WORK_LOG.md`, `README.md`
- **Не менялись:** код, прототип (`prototype/`), данные исследований (`research/`), workflow.

### Что проверено/протестировано

- Ссылки и якоря: проверены все относительные Markdown-ссылки в `docs/*.md` и `README.md` — **битых 0**
  (включая новые `MVP_SCOPE.md` и ссылки из неё на FEATURES/SECURITY/ADMIN/DATA_MODEL/ADR).
- Структура ADR перепроверена: **28** ADR, все в индексе и в тексте, у всех полный набор обязательных полей
  (ID, дата, статус, решение, причина, последствия); ADR со статусом `Under Discussion` не осталось.
- Поиск посторонних символов (CJK) по всем документам: найдена и исправлена одна опечатка в `MVP_SCOPE.md`
  (§12, пункт 3); после исправления — 0.
- `research/tts/check.py` — **0 ошибок, 0 предупреждений** (контроль: данные исследования не затронуты).
- `git status` подтверждает, что прототип и исследование не изменены этой задачей.
- **Не проверялось:** ничего не запускалось и не реализовывалось; состав среза — предложение, а не факт;
  оценки трудоёмкости **намеренно не давались** (нет ни кода, ни замеров — любые цифры были бы выдумкой).

### Известные проблемы / честные ограничения

- **Состав MVP и Stage 1 не утверждён** — это предложение; решает владелец (PROJECT_PLAN §5, вопросы №8, №31).
- **Стек по-прежнему не выбран**: R1/R2 отложены владельцем, spikes не разрешены → спецификации сознательно
  описаны на уровне данных и поведения, а не технологий (привязка к стеку только там, где без неё нельзя:
  сессии в БД, feature flags).
- **Статус инструментов в прототипе не проверялся поштучно**: в `js/tools.js` отдельным кодом реализован QR,
  остальные идут через общий экран инструмента; комментарий файла прямо говорит «простые функции реализованы,
  сложные — макеты». Требуется уточнение при спецификации 1.0.
- **Email-провайдер не выбран** (вопрос №16) → восстановление доступа и уведомления в 1.0 ограничены;
  это отражено в спецификации (§5.1), а не скрыто.
- Несколько продуктовых вопросов остались открытыми и вынесены в чек-лист: повторения задач в 1.0 (§5.3),
  овердрафт в учёте (§5.6), форматирование заметок (§5.5), дефолты утро/вечер (№9), тема и адаптивность (№32, №34).
- **Сбой окружения (не проекта):** локальный git-клон дважды за сессию пересоздавался на базовом коммите
  `c091d26`, из-за чего уже закоммиченные файлы выглядели как незакоммиченные изменения. Коммиты на GitHub
  не пострадали; ветка восстановлена через `git fetch origin +refs/heads/arena/01a0da18-aven:...` и
  `git reset --mixed origin/arena/01a0da18-aven`, после чего рабочее дерево совпало с ожидаемым.
- По-прежнему ждут владельца: прослушивание образцов (PR #6, `voice-lab-name.html`), ★5 у `vd17-design`,
  архитектура TTS (свой GPU / аренда / System TTS).

### Что рекомендуется делать следующим

1. **Владельцу:** ответить на чек-лист `MVP_SCOPE.md` §12 (16 вопросов, достаточно коротких ответов по номерам)
   и на оставшиеся пункты чек-листа `STACK_RESEARCH.md` §13 (кроме отложенных R1/R2). После этого:
   ADR → `Accepted`, состав MVP → утверждён, Stage 1 разблокирован, spikes S1–S5 можно разрешать.
2. **Агенту безопасно делать до решений владельца** (документация и прототип, без production-кода):
   доработать прототип под срез 1.0 — экраны входа/регистрации/2FA, отдельный экран истории действий с Undo,
   минимум админки (`/admin`: users, роли, аудит, миграции, health, backup), пустые состояния и подтверждения
   опасных действий, тема и адаптивность (вопросы №32–35); это закрывает пробелы «нет в прототипе» из §4.1.
3. **Агенту безопасно:** черновик схемы данных под срез 1.0 в `DATA_MODEL.md` (сущности, поля, связи, JSONB-
   расширения, история/Undo, аудит) — как проект, без кода и миграций.
4. После утверждения стека — spikes S1–S5 (`STACK_RESEARCH.md` §12) и только затем реализация.
5. Голос (Stage 3) Stage 1 не блокирует: вернуться к TTS после прослушивания PR #6.

## 2026-09-26 — XVII. Stage 1: материалы для решения по стеку (исследование, ADR-предложения)

- **Дата:** 2026-09-26
- **Задача:** продолжить работу по плану. По плану Stage 1 («полезный самостоятельный веб-сайт») начинается
  **после** перевода ADR из `Proposed` в `Accepted` (PROJECT_PLAN §4), а утверждение — за владельцем;
  ADR-101 сам помечал «сравнение альтернатив» незакрытым, открытые вопросы №3/№4/№5 — про то же.
  Задача агента на этом шаге: **подготовить материалы, чтобы решение можно было принять за один проход**.
  Решения не принимались, production-код не писался, зависимости не устанавливались (AGENT_GUIDE §8).

### Что конкретно сделано

- **Новый документ `docs/STACK_RESEARCH.md`** (по образцу TTS_RESEARCH.md) — исследование стека Stage 1:
  - **§2** требования к стеку, **вытекающие из наших же спецификаций** (16 позиций с источниками: ADR-002/003/
    005/006/007/008/010/011/012/013, SECURITY §1/§2/§5/§7/§10/§11, ADMIN §5/§13/§16–18/§21, DATA_MODEL §3.2,
    ARCHITECTURE §3–4, PROJECT_PLAN §6.2, FUTURE) — стек обязан им удовлетворять, это не пожелания.
  - **§3 четыре развилки**, которые определяют остальное: **R1** где живёт backend (отдельный API + веб-клиент —
    рекомендация, против full-stack Next.js / React Router v7 framework mode; аргумент — единый Action Core
    ARCHITECTURE §4 и будущие клиенты Stage 5–6); **R2** где хостим (свой VPS + Docker Compose — рекомендация;
    PaaS; гибрид с managed БД; Kubernetes — не рекомендуется); **R3** сколько компонентов вводим сразу
    (принцип «каждый компонент по обоснованию»); **R4** модульный монолит вместо микросервисов.
  - **§4–7** сравнение альтернатив с версиями и лицензиями на дату исследования: runtime (Node 24 LTS —
    EOL 2028-04-30; Node 26 → Active LTS 2026-10-28; TypeScript 6.x, 7.0 — Go-компилятор от 2026-07-08,
    пилот в CI), фреймворк (NestJS 11.x / Fastify 5.x / Hono 4.x / Express 5), контракт API (REST+OpenAPI+Zod /
    tRPC / GraphQL), доступ к данным (Drizzle 0.45.x / Prisma 7.x / Kysely), СУБД (PostgreSQL 18.4 — текущая,
    17.x поддерживается, 14 — EOL ноябрь 2026), очередь (pg-boss / BullMQ / Temporal / cron), кэш (обоснован
    **отказ** до измерений; Valkey 9.1 BSD против Redis 8.2 AGPLv3), файлы (MinIO — публичный репозиторий
    **архивирован** 2026-04-24; SeaweedFS/RustFS Apache-2.0, Garage AGPL-3.0, Ceph LGPL, managed),
    аутентификация (Better Auth MIT встроенная / Auth.js v6 / Zitadel Apache-2.0 / Keycloak / своя — не
    рекомендуется), веб-клиент (Vite+React+TanStack / React Router v7 / TanStack Start 1.0 / Next.js 16.3.x),
    UI (headless Radix/React Aria + CSS из прототипа — доступность не доделывается потом), инструменты
    (pnpm workspaces, Vitest, Testing Library, Playwright, ESLint+Prettier или Biome, GitHub Actions).
  - **§8** стоимость владения: self-host Stage 1 ≈ **€10–20/мес** (Hetzner CX22 ≈ €3.29, CX32 ≈ $7.49,
    регионы Германия/Финляндия/США, бэкапы +20%), домен, бэкап-хранилище, managed-БД как опция;
    GPU для TTS на Stage 1 **не требуется** (голос — Stage 3, TTS_RESEARCH §12–13).
  - **§9** два готовых набора («минимальный» — рекомендуется, и «платформенный» с Next.js) + явный список того,
    что **откладывается** (Valkey, отдельный IdP, Kubernetes, полный observability-стек, offline-sync, AI Router,
    TTS-сервер, мобильный/desktop-стеки).
  - **§10** риски и смягчение (критические CVE в Next.js 16.3.3/15.5.24 в сентябре 2026 → pinned LTS + дисциплина
    патчей; архивный MinIO; AGPL у Redis 8; иная диагностика TS 7; отсутствие дашбордов у pg-boss; вендор-лок).
  - **§11** сводка лицензий рекомендованного набора (все пермиссивные; AGPL-компоненты в набор не входят).
  - **§12** план проверок **S1–S5** с критериями успеха — и честное указание, что проверки требуют кода,
    поэтому сейчас **не выполнялись**; предлагается явное разрешение владельца и папка `research/stack-spike/`
    (не production-код), как ранее было разрешено исследование TTS.
  - **§13** чек-лист из **16 вопросов владельцу** (ответы по номерам) + **§14** ограничения честности
    + **§15** источники с датами и URL.
- **DECISIONS.md расширен до decision-ready вида** (статусы решений **не** повышались до `Accepted`):
  - ADR-101, 102, 103, 108 дополнены полями «Альтернативы (сравнение)», «Что проверить перед `Accepted`»,
    точными версиями и ссылками на разделы STACK_RESEARCH; открытый вопрос ADR-101 («обоснование framework
    против «лёгкого собственного сервера»») закрыт материалами — выбор остался за владельцем.
  - **ADR-106** и **ADR-107** переведены из `Under Discussion` в `Proposed`: подготовлены сравнение альтернатив
    и обоснование (очередь — pg-boss в PostgreSQL с явными критериями перехода на BullMQ+Valkey; кэш-сервер —
    **не вводится** до измеренной необходимости, при необходимости Valkey, а не Redis 8). Секция переименована
    в «Ранее `Under Discussion` — переведены в `Proposed`», чтобы история статусов не потерялась.
  - Добавлены **новые ADR-предложения**: **ADR-109** (Drizzle ORM + drizzle-kit), **ADR-110** (Better Auth +
    свой RBAC и аудит), **ADR-111** (REST + OpenAPI + общие Zod-схемы), **ADR-112** (Vitest/Testing Library/
    Playwright/интеграционные на реальной БД), **ADR-113** (монорепо pnpm workspaces + Docker Compose на VPS +
    GitHub Actions + бэкап с проверенным восстановлением). Все — `Proposed`.
  - ADR-104/105 (mobile/desktop) честно помечены: исследование стека 2026-09-26 их **не покрывает** (Stage 5–6).
- **PROJECT_PLAN.md:** раздел 8 «Предварительный технологический вектор» переписан — таблица из 15 областей
  со ссылками на ADR и материалы для решения (вместо 9 строк «требует ADR»); открытые вопросы №3, №4, №5
  уточнены (материалы подготовлены, решение за владельцем); добавлены вопросы **№36–39** (провайдер/регион/
  бюджет/домен; наблюдаемость; линтер и состав CI-проверок; разрешение на spikes); STACK_RESEARCH.md добавлен
  в карту документации (§3); дата документа обновлена.
- **ARCHITECTURE.md:** §14 дополнен — ссылка на STACK_RESEARCH и пять архитектурных следствий, зафиксированных
  как **предложения** (доменная логика в `packages/core`, а не в UI-слое; модульный монолит; минимум
  инфраструктурных компонентов; автоматизации без произвольного JS; новые ADR-109…113). Дата обновлена.
- **README.md и CHANGELOG.md (XII):** новый документ в карте; запись о материалах, изменениях ADR и фактах,
  которые повлияли на рекомендации. В CHANGELOG (XI) исправлена цифра: метрика имени посчитана по **464**
  гипотезам (было указано 446 — значение до CI-прогона).

### Какие файлы изменены

- Создано: `docs/STACK_RESEARCH.md`
- Обновлено: `docs/DECISIONS.md`, `docs/PROJECT_PLAN.md`, `docs/ARCHITECTURE.md`, `docs/CHANGELOG.md`,
  `docs/WORK_LOG.md`, `README.md`
- Код, прототип, данные исследований и workflow **не менялись**.

### Что проверено/протестировано

- Структура ADR: скриптом проверено, что все **28** ADR присутствуют и в индексе, и в тексте (расхождений нет),
  и что у каждого есть обязательные поля формата (ID, дата, статус, решение, причина, последствия) —
  [AGENT_GUIDE.md](AGENT_GUIDE.md) §5. Нарушений: 0.
- Ссылки: проверены все относительные Markdown-ссылки в `docs/*.md`, `README.md`, `prototype/README.md` —
  **битых 0** (включая новые ссылки на STACK_RESEARCH.md и якоря ADR-109…113).
- Согласованность статусов: в DECISIONS.md не осталось ADR со статусом `Under Discussion`; индекс и тела
  совпадают; в PROJECT_PLAN §8 все строки ссылаются на существующие ADR.
- **Не проверялось (и не могло быть на этом этапе):** ничего не устанавливалось, не собиралось и не запускалось;
  версии/лицензии/цены взяты из внешних источников (раздел 15 STACK_RESEARCH) и не перепроверялись по
  официальным реестрам — это явно записано в ограничениях честности (§14) и подлежит фиксации на момент `Accepted`.

### Известные проблемы / честные ограничения

- **Ни одно решение не принято.** Все ADR — `Proposed`; STACK_RESEARCH содержит **рекомендации агента**, а не
  решения владельца. Реализация до `Accepted` запрещена (AGENT_GUIDE §1.2, §8).
- **Версии и цены устареют**: данные на 2026-09-26 из сторонних публикаций и официальных страниц; часть
  источников — SEO-статьи с маркетинговыми бенчмарками (цифры throughput **не** перепроверялись и для Aven
  не измерялись — это записано в документе).
- **Юридическая оценка лицензий не проводилась** (в частности AGPLv3 у Redis 8 и Garage) — указана только
  фактическая лицензия; для self-hosted без распространения это обычно приемлемо, но оценка — за владельцем.
- **Spikes не выполнены** (требуют кода → на этапе 0 запрещены): план S1–S5 описан, но ни одна проверка
  не подтверждена практикой.
- Продуктовые вопросы, без которых Stage 1 всё равно не начать, **не решены**: состав MVP (№8), какие разделы
  входят в Stage 1 (№31), тема/адаптивность/навигация (№32–35), email-провайдер (№16), каналы уведомлений (№17).
- Открытые вопросы безопасности, влияющие на реализацию, остались открытыми: механизм защиты критического
  audit trail (№14), политика обязательности 2FA по ролям (SECURITY §11), перечень чувствительных настроек
  (№19), механизм повторной аутентификации (№21).
- Всё это по-прежнему ждёт владельца по голосу (этап 2/2.1): прослушивание, ★5 у `vd17-design`, выбор
  архитектуры TTS (свой GPU / аренда / System TTS) — PR #6.

### Что рекомендуется делать следующим

1. **Владельцу:** ответить на чек-лист из 16 вопросов (STACK_RESEARCH §13) — достаточно коротких ответов по
   номерам (например: «1 — Б; 2 — VPS Hetzner, Финляндия; 4 — Fastify; …»). После этого ADR-101…113
   переводятся в `Accepted`, и Stage 1 разблокирован.
2. **Владельцу:** решить продуктовые вопросы №8 и №31 (состав MVP и разделы Stage 1) — стек без них не даёт
   старта; при необходимости подготовлю такой же «пакет решений» по составу разделов (материал — FEATURES §2).
3. **Владельцу:** разрешить spikes S1–S5 (или только S1+S3) в `research/stack-spike/` и задать критерий времени
   восстановления из бэкапа (S3).
4. До решений владельца агенту безопасно делать только документацию/прототип: уточнить спецификацию разделов
   Stage 1 (формы, поля, валидация, состояния, a11y) на основе `prototype/` и FEATURES §2; подготовить черновик
   DATA_MODEL-схемы под выбранный срез (без кода); доработать прототип по вопросам №32–35 (тема, адаптивность,
   структура навигации профиля).
5. Голос (Stage 3) не блокирует Stage 1: вернуться к TTS после прослушивания PR #6.

## 2026-09-25 — XVI. Голос Aven, этап 2.1: проверены способы произношения «Авен» + метрика имени

- **Дата:** 2026-09-25
- **Задача:** продолжить работу по плану (WORK_LOG XV, п. 1: «способы исправления произношения описаны,
  но не проверены — это следующий шаг»; TTS_RESEARCH §11.2). Проверить три описанных способа реальным
  прогоном, добавить объективную метрику произношения имени, не внедрять голос и не раздувать репозиторий.
  Решения за владельца не принимались; ADR не создавался.

### Что конкретно сделано

- **Новый набор фраз** `research/tts/phrases_name.json` (18 клипов n00–n25, 5 групп): эталонный клип
  VoiceDesign (n00 — текст T1, тот же `DESIGN_PROMPT`), способ 3 «перебор орфографии/контекста»
  (n01 «Авен» = контроль, n02 «Авин», n03 «А-вен», n04 «Меня зовут Авен.», n05 прайминг повтором),
  способ 2 «подсказка в промпте голоса» (n06, n07 — `DESIGN_PROMPT + NAME_HINT`), способ 1
  «in-context learning» — клоны `0.6B-Base` (n10–n13) и **`1.7B-Base`** (n20–n25) по эталону n00
  (включая короткие реплики «Слушаю.»/«Готово.», числа и ГРМ). Способ 4 («готовый образец») генерации
  не требует — описан как механизм продукта. Базовые `phrases.json` (T1–T10) и `phrases_vd17.json` (xNN)
  **не менялись** (добавлены только метаданные-якоря и поле `target`), сравнимость прежних прогонов сохранена.
- **Генератор** `research/tts/gen_qwen3_name.py`: три модели одним прогоном, `create_voice_clone_prompt`
  по эталонному клипу (0,4 с), F0 по каждому клипу, ошибки фразы не роняют прогон.
- **Прогон** в GitHub Actions (run **36184248453**, engine `qwen3-name`, CPU AMD EPYC 7763, 4 vCPU,
  15,6 ГБ RAM, без GPU): **18/18 клипов, 0 ошибок**, 471,5 с синтеза; загрузки моделей 20,3 / 11,7 / 20,7 с.
  Образцы → `prototype/assets/voice-samples/qwen3/{vd17-design,base06-clone,base17-clone}/nNN.mp3`
  (**+320 КБ**, 18 файлов; всего 232 MP3 ≈ 6,6 МБ).
- **Новая объективная метрика имени** (`asr_eval.py` переработан): `hyp_norm`, `twer` (против желаемого
  звучания), `lwer` (побуквенно) и **`name.letters` / `name.lwer`** — имя вырезается из гипотезы Whisper
  по якорям `name_prev`/`name_after` (difflib + окно по Левенштейну, допуск 3 буквы) и сравнивается с «авен».
  Метрики производные: считаются из уже сохранённых гипотез (`python asr_eval.py --derive`), повторное
  распознавание не нужно — посчитаны для всех 464 клипов в `results/asr.json`; метрика имени — на 91 клипе (79 прежних + 12 новых), ошибок выделения **0**.
- **Поправка к выводу этапа 2 (честно):** «ни один из четырёх вариантов не даёт эталонного «Авен»» было
  следствием **словесного** WER (Whisper пишет «А вен» двумя токенами). По `name_lwer` **x18/n01/n03/n04/n07 —
  точное «авен» (0%)**; хуже делают «Авин» → «Арин» (50%), прайминг → «Авин», подсказка в промпте на короткой
  реплике → «Афин» (50%), а также прежние «Авэн» → «авон» и «А́вен» → «айф». §11.2 помечен поправкой,
  историческая таблица сохранена.
- **Результаты клонов:** `0.6B-Base` точно говорит «Авен» в развёрнутой фразе (n11 + t01 этапа 1 — две
  независимые генерации) и чисто произносит «Слушаю.» (0% против полного провала x03 у VoiceDesign);
  `1.7B-Base` по имени хуже («Авент», «Овен»), но числа/ГРМ — корректны. Тембр клонов **на слух не сравнивался**.
- **Пересчёт всех клипов T1 этапа 1** по метрике имени → сводка `name_pronunciation` (75 голосов):
  точное «авен» дают base06-clone, supertonic/F2 и vosk spk00/01/03/04 (последние в git не хранятся по лицензии);
  большинство голосов — «Авин» (25%).
- **Страница прослушивания** `prototype/voice-lab-name.html`: группы по способам, «Сравнить способы на одной
  фразе» (6 вариантов «Aven слушает вас.» подряд), ★ за каждый вариант и общие ★ за голоса-участники
  (пишутся в тот же ключ, что в voice-lab), кнопка «Системный голос», метрики CPU/F0, гипотеза Whisper
  и вырезанные буквы имени, таблица «кто как говорит «Авен»», честное пустое состояние, если прогон ещё не делался.
  Ссылки добавлены в `voice-lab.html` и `voice-lab-vd17.html` (+ побуквенная метрика в карточках x17–x20).
- **Пайплайн:** `collect.py` строит в `manifest.js` блок `name` (варианты, метрики, ASR, сводка произношения);
  `check.py` проверяет новый набор фраз, политику publish и ссылки манифеста; `candidates.json` — голос
  `base17-clone` в publish; workflow получил engine `qwen3-name` (в `ENGINES` по умолчанию не входит — точечный прогон).
- **Исправлена регрессия этапа 2:** `phraseSet` считался по общему числу id, из-за чего голоса с полным набором
  T1–T10 помечались «key», а «Настройки → Голос» подписывали их «образцы T1, T3, T4, T6». Теперь набор
  определяется по T-фразам (`full` / `key` / `t01` / `extended` / `name`), добавлены поля `phraseSets`
  и `sampleNote` (подпись берётся из манифеста), а голоса только из мини-прогона (`nameTestOnly`)
  не предлагаются для озвучки реплик и не мешают в общем A/B-сравнении.
- **Документация:** TTS_RESEARCH §16 (методика, метрика, таблица 18 клипов, пересчёт этапа 1, выводы,
  ограничения), поправка в §11.2, строка CPU-измерений в §12.1, практические заметки о пути клона в §13,
  обновлённые вопросы владельцу в §15; VOICE.md §10.3; CHANGELOG (XI); prototype/README.md; эта запись.
- Голос **не внедрялся**: `speechSynthesis` остаётся движком по умолчанию и fallback. Production-код,
  backend, зависимости приложения не добавлялись; ADR не создавался.

### Какие файлы изменены

- Создано: `research/tts/phrases_name.json`, `research/tts/gen_qwen3_name.py`, `research/tts/tests/dryrun_name.py`,
  `prototype/voice-lab-name.html`
- Обновлено: `research/tts/{common,asr_eval,collect,check}.py`, `research/tts/candidates.json`,
  `research/tts/ENGINES`, `research/tts/phrases.json` (якоря имени), `research/tts/phrases_vd17.json`
  (`target` + якоря у x00/x17–x20), `.github/workflows/tts-research.yml` (engine `qwen3-name`)
- Прототип: `prototype/voice-lab.html`, `prototype/voice-lab-vd17.html`, `prototype/js/tts/providers.js`,
  `prototype/js/settings.js`, `prototype/assets/voice-samples/manifest.js`, **+18 MP3** (n00–n25)
- Данные прогона (коммит бота 38ca0f3): `research/tts/results/qwen3_name.json`, `results/asr.json`,
  `results/logs/qwen3-name.{log,lscpu.txt}`
- Документы: `docs/TTS_RESEARCH.md`, `docs/VOICE.md`, `docs/CHANGELOG.md`, `docs/WORK_LOG.md`, `prototype/README.md`
- Временно создавалось и **удалено**: `research/tts/ENGINES_PUSH` + push-триггер workflow (нужны были,
  потому что GitHub App token в песочнице не может дёргать dispatch); workflow снова запускается только вручную.

### Что проверено/протестировано

- `research/tts/tests/dryrun_name.py` — локальный прогон генератора на заглушках torch/qwen_tts (без модели
  и GPU): 18/18 WAV по правильным каталогам голосов, 3 загрузки моделей, 2 clone prompt, ровно один
  `first_call` на голос — **PASS**.
- GitHub Actions run 36184248453: plan ✅ (выбран только `qwen3-name`), generate ✅, collect ✅
  (0 удалений, 18/18 вариантов в манифесте, ASR посчитан по всем новым клипам).
- `python research/tts/check.py` — **0 ошибок, 0 предупреждений** (10 + 21 + 18 фраз, наборы не пересекаются,
  манифест = 232 ссылки = 232 MP3 на диске, `extended` 21/21, `name` 18/18).
- `python -m py_compile` всех скриптов `research/tts` (включая `tests/`); `node --check` всех JS прототипа;
  `node research/tts/normalize.test.js` — 26/26; YAML workflow разбирается, триггеры — только
  `workflow_dispatch` и `repository_dispatch`.
- jsdom + локальный статический сервер: 26/26 проверок страниц — пустое состояние (репозиторий до прогона),
  18 карточек и 5 групп после прогона, метрики/ASR-строки/пиллы, ★ за вариант и за голос → localStorage,
  `base17-clone` не попадает в общее сравнение и в список озвучки, ссылки между страницами.
- Локальный сервер прототипа поднят (0.0.0.0:8080) — владелец может слушать образцы в preview песочницы.

### Известные проблемы / честные ограничения

- **ASR ≠ ухо.** `name_lwer` ловит замену букв, но не ударение и паузу: «А вен» (n01/n03/x18) побуквенно
  точно, однако может звучать как два слова. Окончательная оценка — только прослушиванием владельцем.
- **Тембр клонов не сравнивался на слух** (n10–n13, n20–n25); `1.7B-Base` участвовал в нашем наборе впервые.
- **Подсказка в промпте (n06/n07) меняет и тембр**: эти клипы нельзя подставлять вместо ★5-эталона без
  переслушивания голоса целиком.
- **По одной генерации на вариант**; устойчивость подтверждается только совпадением n01 и x18 (разные прогоны,
  разные CPU) и n11 с t01 этапа 1.
- **`first_call` у n01/n06 в `results/qwen3_name.json` — артефакт** группировки генератора (первый клип группы,
  а не холодный старт); в генераторе исправлено, метрики прогона задним числом не правились.
- Все цифры — **CPU** (RTF 6,4–12,0), не целевая среда; GPU в окружении нет, Hugging Face из песочницы недоступен.
- `repository_dispatch` по-прежнему недоступен из песочницы (токен без прав на Actions) — ручной запуск
  владельцем либо временный push-триггер.

### Что рекомендуется делать следующим

1. **Владельцу:** открыть `prototype/voice-lab-name.html` (preview песочницы или GitHub Pages после мержа) и
   послушать: (а) «Aven слушает вас.» — 6 вариантов подряд (устраивает ли «А вен» по ударению/паузе);
   (б) кандидатов на готовый образец приветствия — n04 «Меня зовут Авен.», n07, n11; (в) тембр клонов
   n10–n13 / n20–n25 против ★5-эталона vd17-design.
2. **Владельцу:** подтвердить/снять ★5 у vd17-design по расширенному набору (§11) и выбрать путь архитектуры
   (§13): свой GPU-сервер / аренда GPU / пока остаться на System TTS.
3. Если готовый образец приветствия утверждается — описать механизм «уровня 0» (готовые реплики + кэш)
   в VOICE.md как часть будущего TTS-слоя (это не требует GPU и не меняет экранный текст «Aven»).
4. После выбора голоса — измерить latency на реальном GPU (TTFB, полное время, прогрев, VRAM) и только потом
   решать о внедрении; System TTS остаётся fallback при любом сценарии.
5. Вернуться к продуктовым вопросам плана (открытые вопросы №31–35, ADR по стеку) — голос не блокирует Stage 1.

## 2026-09-25 — XV. Голос Aven, этап 2: расширенный тест vd17-design, GPU/latency, проект подключения и lip-sync

- **Дата:** 2026-09-25
- **Задача:** по решению владельца (vd17-design — ★5, предварительно): (1) проверить фаворита на реальных
  сценариях Aven и отдельно — на произношении «Авен»; (2) исследовать практическую скорость Qwen3 на GPU;
  (3) спроектировать вариант подключения к Web Aven (без объявления production-архитектуры);
  (4) исследовать, что нужно для будущего lip-sync 3D; (5) голос **не** внедрять как обязательный,
  System TTS оставить fallback, репозиторий не раздувать.

### Что конкретно сделано

- **Расширенный набор фраз** `research/tts/phrases_vd17.json` (21 реплика, 9 групп): короткие реплики,
  напоминания/время, деньги, авто (пробег, заправка, ГРМ), вопрос, предупреждение, развёрнутый ответ
  и 4 варианта произношения «Aven / Авен» (латиница без нормализации · кириллица «Авен» · со знаком
  ударения «А́вен» · через «э» «Авэн»). Базовый `phrases.json` (T1–T10) **не** изменён — сравнимость
  движков этапа 1 сохранена.
- **Генератор** `research/tts/gen_qwen3_vd17.py`: прогон самим `Qwen3-TTS-12Hz-1.7B-VoiceDesign`
  (промпт описания голоса вынесен в `common.py` → `DESIGN_PROMPT`, чтобы этап 1 и этап 2 не разошлись);
  фраза x00 повторяет текст T1 для проверки воспроизводимости. Метрики → `results/qwen3_vd17.json`.
- **Прогон** на CPU-раннере GitHub Actions (run **36176203476**, 4 vCPU Xeon 8370C, без GPU):
  21/21 фраз, 0 ошибок; загрузка модели 20,0 с; медиана синтеза 18,5 с, RTF 6,43, максимум 70,1 с
  (длинный ответ); F0 по всем клипам 207–318 Гц. Образцы → `prototype/assets/voice-samples/qwen3/vd17-design/xNN.mp3`
  (**+508 КБ**, моно 24 кГц 48 кбит/с — те же правила, что у остальных образцов; всего 214 MP3 ≈ 6,2 МБ).
- **ASR-проверка** (`asr_eval.py` расширен на xNN): 11 реплик из 17 распознаны Whisper с WER 0%;
  числа, деньги, пробег, даты и буквенное чтение ГРМ — корректны.
- **«Авен»:** латиница без нормализации → «Айвен» (ASR: «Ай, вен слушит вас!») — нормализация обязательна;
  «Авен» (текущий словарь) — лучший из четырёх, но ASR слышит «А вен»; знак ударения и «Авэн» — хуже
  («Айф…», «А вон…»). Экранный текст «Aven» не менялся нигде — меняется только то, что уходит в TTS.
- **Страница прослушивания** `prototype/voice-lab-vd17.html`: группы по сценариям, отдельный блок
  «произношение Aven/Авен» с проигрыванием 4 вариантов, ★ за каждую фразу, проигрывание группы/всех
  подряд, кнопка «Системный голос» для сравнения с fallback, честная плашка «образцы ещё не сгенерированы»
  при отсутствии данных. Ссылка на неё добавлена в `voice-lab.html` (+ метка «★ фаворит» у vd17-design);
  в `voice-lab` общие оценки голоса переиспользуют тот же localStorage-ключ.
- **Пайплайн:** `collect.py` собирает в `manifest.js` блок `extended` (фразы, метрики, ASR-гипотезы, env);
  `check.py` (новый) проверяет наборы фраз, политику `publish` и целостность манифеста, добавлен шагом в workflow;
  `common.py` считает сводку и для x-фраз; workflow получил engine `qwen3-vd17` и `repository_dispatch`
  (мануальный dispatch из песочницы недоступен: GitHub App token без прав на Actions).
- **Исследование (документы):** `docs/TTS_RESEARCH.md` §11 (результаты расширенного теста и разбор «Авен»),
  §12 (наши CPU-измерения **отдельно** от опубликованных данных Qwen: 97–101 мс до первого пакета,
  RTF 0,29–0,46, таблица 2 arXiv:2601.15621; VRAM по размеру весов HF: 4,5 ГБ для 1.7B / 2,5 ГБ для 0.6B;
  streaming — только через vLLM-Omni, пакет `qwen-tts` 0.1.1 его не даёт), §13 (варианты подключения:
  свой GPU-сервер / аренда GPU / в браузере / готовые образцы + кэш / fallback System TTS — с latency,
  приватностью, стоимостью и сложностью), §14 (lip-sync: Qwen3 тайминги **не** отдаёт — проверено по
  исходникам пакета; план: forced alignment → визимы; Rhubarb MIT / MFA MIT / WhisperX BSD-2 и др.
  с лицензиями), §15 (статус: кандидат, не зависимость; обновлённые вопросы владельцу).
- **VOICE.md** §10.2, **CHANGELOG** (X), **prototype/README.md** обновлены.

### Какие файлы изменены

- `research/tts/phrases_vd17.json` (новый), `research/tts/gen_qwen3_vd17.py` (новый), `research/tts/check.py` (новый)
- `research/tts/common.py`, `research/tts/collect.py`, `research/tts/asr_eval.py`, `research/tts/gen_qwen3.py`,
  `research/tts/candidates.json` (оценка владельца ★5 как «предварительная, не утверждение»), `research/tts/ENGINES`
- `.github/workflows/tts-research.yml` (engine `qwen3-vd17`, `repository_dispatch`, шаг проверки)
- `prototype/voice-lab-vd17.html` (новый), `prototype/voice-lab.html`, `prototype/assets/voice-samples/**` (+21 MP3),
  `prototype/assets/voice-samples/manifest.js` (блок `extended`), `prototype/README.md`
- `docs/TTS_RESEARCH.md`, `docs/VOICE.md`, `docs/CHANGELOG.md`, `docs/WORK_LOG.md` (эта запись)
- `research/tts/results/qwen3_vd17.json`, `research/tts/results/asr.json` (коммит бота из run 36176203476)

### Что проверено/протестировано

- `python research/tts/check.py` — 0 ошибок, 0 предупреждений (21/21 расширенных фраз, манифест = 214 ссылок =
  214 MP3 на диске, всё в политике `publish`).
- `python -m py_compile` всех скриптов research/tts; `node research/tts/normalize.test.js` — 26/26;
  `node --check` для JS прототипа; `manifest.js` разбирается Node и содержит корректный блок `extended`.
- GitHub Actions run 36176203476: plan ✅, generate (qwen3-vd17) ✅, collect ✅ (0 удалений, 21/21 фраз,
  ASR посчитан по всем новым клипам).
- Воспроизводимость: промпт описания голоса — один на оба генератора (`DESIGN_PROMPT` в `common.py`).

### Известные проблемы / честные ограничения

- **GPU в окружении нет** (песочница без GPU, Hugging Face заблокирован): все наши цифры — CPU.
  GPU-показатели — опубликованные данные Qwen/vLLM, помеченные как чужие; на целевом железе не проверены.
- **Произношение «Авен» не решено**: по ASR ни один из 4 вариантов не даёт эталонного «Авен»;
  требуется прослушивание владельцем. Способы (ICL-клон по эталонному клипу, подсказка в промпте,
  перебор орфографии, готовый образец) описаны, но **не** проверены — это следующий шаг.
- **Стабильность тембра** между вызовами проверена частично (x00 воспроизводит длительность T1 — 5,20 с,
  но оба клипа — первые после загрузки с фиксированным seed); полный ответ только на слух.
- Сверхкороткая реплика «Слушаю.» (x03) не распознана ASR вовсе («А, слушай!») — слушать в первую очередь.
- `repository_dispatch` заработает только после попадания workflow в `main`: GitHub берёт файл workflow
  с ветки по умолчанию. В этой сессии прогон запускался временным push-триггером (удалён).

### Что рекомендуется делать следующим

1. Владельцу: прослушать `prototype/voice-lab-vd17.html` — подтвердить/снять ★5, выбрать вариант
   произношения «Авен» (или дать добро на мини-прогон ICL-клона/перебора орфографии).
2. Владельцу: выбрать путь по архитектуре (§13): свой GPU-сервер / аренда GPU / оставить System TTS.
3. После утверждения голоса — измерить latency на реальном GPU (TTFB, полное время, прогрев, VRAM)
   и только потом решать о внедрении. System TTS остаётся fallback при любом сценарии.
4. 3D/lip-sync — отдельный этап после выбора голоса (§14, не начинать сейчас).

---

## 2026-09-25 — XIV. PR #4: очистка TTS-образцов (вариант Б), workflow только вручную

- **Дата:** 2026-09-25
- **Задача:** по решению владельца (вариант Б) убрать из PR #4 лишние исследовательские MP3, сохранить удобный voice-lab, документацию и воспроизводимость; убрать автозапуск workflow; не выбирать голос/архитектуру.

### Что конкретно сделано

- Синхронизация с remote: локальная ветка переведена на `313531a` (bot-коммит run 36168134501 с обновлёнными образцами/метриками Chatterbox — сохранён).
- `research/tts/candidates.json`: новая политика `publish` (что попадает в git); заголовок Chatterbox без «(голос по умолчанию)».
- `research/tts/collect.py` переписан: метрики/логи всех движков → `results/`; MP3 — только по `publish`; чистка лишних MP3; `manifest.js` строится заново по реально существующим файлам (`phraseSet`: full / key / t01).
- Удалено 232 MP3: Silero v5_5_ru (NC) 30, Piper Irina 10, Vosk 23, RHVoice Anna/Irina 20, Silero `__nostress` 18, мужские/низкие голоса (каталог T1) 11, `ru_igor` 10, Silero CIS T2/T5/T7–T10 у 17 голосов 102, eSpeak T2/T4–T10 8. Осталось 193 MP3 (≈5,2 МБ).
- `prototype/voice-lab.html`: основная сетка — голоса с образцом выбранной фразы (+ пометка, сколько скрыто и почему), переключатель eSpeak вместо «NC/неясные».
- `prototype/js/tts/providers.js`: если сохранённый натуральный голос удалён из набора — берётся первый доступный; `settings.js`: пометка «образцы T1, T3, T4, T6» у Silero.
- `.github/workflows/tts-research.yml`: удалён триггер `push`, остался `workflow_dispatch`; `git add -A` фиксирует и удаления; `research/tts/ENGINES` — список по умолчанию для ручного запуска.
- Документы: `docs/TTS_RESEARCH.md` (§2 компактный набор и воспроизводимость, актуальные метрики Chatterbox run 4), `docs/VOICE.md`, `docs/CHANGELOG.md`, `prototype/README.md`, эта запись.

### Что проверено

- `node research/tts/normalize.test.js` — 26/26; `node --check` всех js прототипа; `py_compile` скриптов; YAML workflow разбирается, триггер — только `workflow_dispatch`.
- `collect.py` с фиктивными WAV запрещённых голосов/фраз — ничего не добавлено; подброшенные лишние MP3 — удалены; манифест не изменился.
- jsdom: 33 голоса / 193 ссылки в manifest — 0 битых; запрещённых голосов нет ни в voice-lab, ни в настройках (29 натуральных голосов); сохранённый удалённый голос (piper) → подставлен доступный; Silero T4 — образец; Silero T5 — честный fallback на системный голос.
- Headless Chromium: voice-lab отображается, аудио загружается, ошибок JS нет.

### Известные ограничения

- Удалённые файлы остаются в истории ветки PR; при **squash merge** в историю `main` они не попадут (историю не переписывал).
- Голос и архитектура не выбраны; merge не выполнялся.

### Что рекомендуется делать следующим

1. Владелец слушает voice-lab, отвечает на вопросы TTS_RESEARCH §9.
2. Merge PR #4 — решение владельца (рекомендуется squash merge).

---

## 2026-09-25 — XIII. Натуральный женский голос Aven: исследование TTS, этап 1 (research + prototype)

- **Дата:** 2026-09-25
- **Задача:** «Продолжаем Aven» — заменить роботизированный браузерный голос на естественный русский женский: исследовать бесплатные / self-hosted TTS (классы A–E), проверить лицензии кода и весов, сгенерировать одинаковые фразы T1–T10 для A/B-прослушивания, подготовить прототип (нормализация, TTSProvider, состояния, кэш, приватность, Настройки → Голос, A/B) и ОСТАНОВИТЬСЯ до выбора владельца.

### Что конкретно сделано

- `research/tts/`: `phrases.json` (T1–T10 + «Готово.»), `common.py` (метрики, F0), генераторы `gen_silero.py` (v5_5_ru NC + v5_cis_base MIT + silero-stress), `gen_supertonic.py`, `gen_qwen3.py` (CustomVoice, VoiceDesign → клон Base), `gen_chatterbox.py`, `gen_piper.py`, `gen_vosk.py`, `gen_cli.py` (eSpeak NG, RHVoice); `collect.py` (WAV → MP3 48 кбит/с + manifest), `asr_eval.py` (Whisper small round-trip WER), `candidates.json`, `server.py` (исследовательский self-hosted TTS-сервер), `normalize.test.js`, `ENGINES`.
- `.github/workflows/tts-research.yml`: генерация на CPU-раннере GitHub (4 vCPU, без GPU), по движку в job, логи/ошибки — в артефакт и аннотации, итог коммитится в ветку (`prototype/assets/voice-samples/`, `research/tts/results/`).
- RHVoice собран из исходников в песочнице; образцы Elena / Dasha / Anna / Irina.
- Прототип: `js/tts/normalize.js`, `js/tts/providers.js`, `voice.js` (маршрутизация через AvenTTS, stop, Esc), `presence.js` (`preparing` — «Готовлю речь…»), `settings.js` (новая категория «Голос»), `data.js` (`voice.engine`, `voice.natural`), `index.html`, `style.css`, `voice-lab.html`; `app.js` — select'ы по `change` (раньше срабатывали по клику при открытии).
- Документация: `docs/TTS_RESEARCH.md` (новый), `docs/VOICE.md` (§9, §10.1), `docs/CHANGELOG.md`, `prototype/README.md`, настоящая запись.

### Что проверено

- Прогоны Actions: run 36160072939 (все движки) и 36162159701 (vosk, chatterbox + ASR) — успешно; образцы и метрики закоммичены ботом.
- Silero (CIS/v5_5_ru), Piper, RHVoice — 0,2–0,3 с на фразу на 4 vCPU; Supertonic ≈ 2 с; Vosk 1,4–1,7 с; Qwen3 на CPU ≈ 24 с. ASR WER нейросетевых голосов ≈ 1–3%, eSpeak — 94%.
- `node research/tts/normalize.test.js` — 26/26; `node --check` по изменённым js; `py_compile` по скриптам.
- jsdom smoke-тест: Настройки → Голос, переключение движка, статус сервера, синтез через self-hosted сервер (RHVoice, ~0,24 с до звука), образец для T1, fallback на системный голос, stop; presence `preparing → speaking → idle`.
- Headless Chromium (@sparticuz/chromium): скриншоты voice-lab и Настроек → Голос, ошибок в консоли нет.

### Известные проблемы / не сделано

- **Chatterbox:** прогоны 1–2 упали на загрузке CUDA-весов на CPU; после исправления (`map_location=cpu`) run 36164238774 прошёл успешно (≈20 с на фразу на CPU, RTF ≈4,5; WER: default 13%, clone 1%). Результат был получен после переподключения GitHub (токен в песочнице временно истекал); локальные коммиты запушены после rebase на коммит бота.
- Vosk 0.10 отсутствует в каталоге — использована 0.9 (лицензия весов не ясна → только сравнение).
- Qwen3 и Chatterbox на GPU не проверялись (GPU нет); streaming в пакете qwen-tts 0.1.1 отсутствует.
- Натуральность голосов агент оценить не может (не слушает аудио) — только метрики; выбор — владелец.
- Нормализатор: косвенные падежи числительных не согласуются.
- ADR не создавался; голос не выбран; production-backend нет.

### Что рекомендуется делать следующим

1. PR открыт/обновлён (merge — только владелец).
2. Владелец слушает `prototype/voice-lab.html` (слепой режим), ставит ★, отвечает на вопросы TTS_RESEARCH §9 («Авен»/«Эйвен», архитектура сервер/браузер/GPU).
3. После выбора — этап 2: endpoint выбранного движка и замер на целевом железе.

---

## 2026-09-25 — XII. Интеграция FINAL Female Aven в hero Главной (UX prototype)

- **Дата:** 2026-09-25
- **Задача:** Одобрение владельца preview получено; интегрировать transparent asset в hero Главной по спецификации (hero-зоны, состояния, speaking/listening, клик, событие, light/dark, mobile, a11y, settings, Assistant), проверить скриншотами, commit+push+PR.

### Что конкретно сделано

- Новый hero Главной (`pages1.js`): grid «top / interact / char»; слева приветствие + подпись Aven + состояние текстом + «Чем помочь?» + cmdbar (input/🎤/→) + suggestions (скрыты по умолчанию) + строка последнего ответа + компактное ближайшее событие («Следующее: Стоматолог · 10:00 · через 1 ч 24 мин») + ссылка Assistant; справа Female Aven (`assets/character/web/female-aven-transparent.png`) без рамки/фона: CSS glow (темо-зависимый), декоративные круги, voice waveform, mask-fade низа бюста; mobile-порядок: приветствие/статус → персонаж → команда → событие.
- `js/presence.js` (новый): отображение состояний idle/listening/thinking/speaking/waiting/success/important текстом (role=status aria-live) + data-state для glow/wave; НЕ второй state engine — состояние выводится из существующих подсистем: voice.js (TTS onstart/onend/onerror → speaking/idle), STT onStart/onEnd/onError → listening/idle (home + assistant), flows (start/next → waiting, done → success/important flash), assistant send → thinking.
- Поведение: клик по персонажу → фокус в поле команды; повторный клик → suggestions; chip → заполняет поле. STT в поле Главной (home-mic) с interim-текстом и честным fallback-тостом при недоступности. Character Off → hero во всю ширину (без пустоты); settings set-toggle по путям character перерисовывает страницу. Reduced motion: glow/wave-анимации отключены (статичные бары).
- Assistant: шапка использует тот же transparent asset (`.char-bust`, круг с radial-подложкой); маленькие аватары и floating не менялись (компактность — по спецификации).
- CSS: новая hero-система в `style.css` (grid areas, glow-переменные light/dark, wave/rings/mask, адаптив 860px); `.char-bust` в `character.css`.
- Проверки: headless Chromium (puppeteer-core + @sparticuz/chromium, libs al2023 вручную — CDN весов/браузеров заблокирован прокси, venv/shotkit вне репозитория): скриншоты desktop light/dark, mobile light/dark, speaking, listening, character off → `review/hero-integration/*.jpg`; интеракционные тесты (sugg hidden, фокус после 1-го клика, sugg после 2-го, chip fill, presence speaking→wave flex→idle, img loaded+alt) — все PASS; `node --check` по всем js.
- Документация: `prototype/README.md` (дерево + hero-раздел), `docs/CHARACTER.md` §7.2, `docs/CHANGELOG.md` (VII), настоящая запись.
- Commit + push в `arena/01a0d8a6-aven`; PR #3 обновлён (merge НЕ делал).

### Что проверено

- Скриншоты визуально: персонаж внутри интерфейса (не «вставленное фото»), края чистые на light/dark, лицо читается, команда не перекрыта; mobile-порядок корректен; character off — полная ширина; speaking — текст «Говорю…» + waveform + glow.
- Примечание: в headless-скриншотах emoji-глифы (🎤 и др.) отображаются «квадратами» — в окружении нет emoji-шрифта; в реальных браузерах глифы штатные (код не менялся).

### Что НЕ сделано (осознанно)

- 3D, Male, AI, новый Command Engine, новые лица/причёски/цвета/макияж, новый ADR — нет.
- Merge PR не выполнялся (по инструкции владельца).

### Что рекомендуется делать следующим

1. Владелец смотрит PR #3 / Pages preview и скриншоты; при правках hero — итерация.
2. После принятия hero — отдельный этап: pipeline настоящей 3D-модели по чек-листу.

---

## 2026-09-25 — XI. FINAL 2D MASTER Female: прозрачность + preview (интеграция ожидает)

- **Дата:** 2026-09-25
- **Задача:** Выбор владельца: MAKEUP B → FINAL FEMALE AVEN утверждена (face + HAIR A + COLOR B + MAKEUP B). Подготовить прозрачную версию из утверждённого master (без регенерации внешности), показать 4 preview, НЕ интегрировать до одобрения.

### Что конкретно сделано

- Master сохранён отдельно: `prototype/assets/character/master/female-aven-reference.jpg` (копия утверждённого `stage3-makeup/makeup-B.jpg`); правило «не перезаписывать master» зафиксировано в README ассетов.
- Прозрачность: pipeline без внешних весов (CDN моделей заблокирован прокси sandbox): MediaPipe Selfie Segmentation 0.10.21 (модель в wheel) → trimap → pymatting closed-form alpha → estimate_foreground_ml (анти-halo) → crop bbox+6px. Результат `prototype/assets/character/web/female-aven-transparent.png` (RGBA 741×700, ~464 КБ). Внешность НЕ регенерировалась: фон удалён у утверждённого изображения, identity pixel-level та же.
- QC: лист краёв (макушка/уши/плечи × белый/чёрный) + preview A (белый), B (почти чёрный), C (реальный фон Главной light #f4f5fa), D (dark theme #0f1117) — без синей/белой каймы, halo, остатков фона; пряди, уши, шея, плечи, одежда сохранены. Копии preview и qc-sheet — `review/3d-character-concepts/final/`.
- Документация: новый `prototype/assets/character/README.md` (файлы, правила, pipeline, план интеграции); `docs/CHARACTER.md` §7 (FINAL 2D MASTER, UX/art decision — НЕ ADR; статус интеграции); `docs/CHANGELOG.md`; настоящая запись.
- Commit + push в `arena/01a0d8a6-aven` (PR #3 обновляется).

### Что проверено

- Визуально: 4 preview + qc-sheet (см. выше); размер/alpha PNG (`identify`: 741×700, RGBA).
- Воспроизводимость: pipeline описан в README ассетов (venv вне репозитория).

### Что НЕ сделано (осознанно)

- Интеграция на Главную НЕ выполнена (ждёт одобрения preview владельцем); код прототипа не менялся; Male нет; 3D нет; внешность не перегенерировалась.

### Что рекомендуется делать следующим

1. Владелец одобряет preview (или указывает проблемы маски → улучшение alpha matte).
2. После одобрения: интеграция на Главную по плану (слева приветствие + «Что сделать?», справа Female без собственного фона, за ней CSS radial indigo/violet glow).

---

## 2026-09-25 — X. 3D-направление, этап 3: макияж при master reference COLOR B (review)

- **Дата:** 2026-09-25
- **Задача:** Выбор владельца: COLOR B зафиксирован; master reference = `stage2-color/color-B.jpg`. Этап 3 (последний визуальный): 4 варианта макияжа, менять только макияж.

### Что конкретно сделано

- Identity-locked makeup edit от `color-B.jpg`: MAKEUP A (Natural), B (Soft Professional), C (Elegant), D (Aven Signature: тонкий холодный indigo-акцент у глаз). Оптимизация 1280px JPG (~93–98 КБ) + сравнительный лист `compare-2x2.jpg` в `review/3d-character-concepts/stage3-makeup/` + README этапа.
- Первая генерация D отклонена мной по спецификации (заметный indigo-smokey вместо «очень тонкого» акцента); регенерирована с whisper-quiet indigo-liner — текущий D соответствует spec.
- Обновлены статусы: `stage2-color/README.md` (завершён, COLOR B в master reference), корневой README review-папки (этап 3 + план после выбора: прозрачный фон → проверка краёв волос → Главная Aven).
- Commit + push в `arena/01a0d8a6-aven` (PR #3 обновляется).

### Что проверено

- Визуальный контроль всех четырёх файлов: одна идентичность и один кадр (лицо/геометрия, глаза и цвет глаз, нос, форма губ, челюсть, кожа/возраст, причёска и цвет волос, одежда, шея/плечи, выражение, поза, камера, свет, композиция); макияж не меняет воспринимаемую форму лица; запрещённое (heavy, огромные ресницы, красная помада, glitter, fantasy, чрезмерный contouring) отсутствует.

### Что НЕ сделано (осознанно)

- Выбор макияжа за владельца не делался; фон не удалялся; интеграции на Главную нет; 3D и Male нет.

### Что рекомендуется делать следующим

1. Владелец: «Беру MAKEUP X» → фиксация FINAL FEMALE AVEN.
2. Затем по спецификации: прозрачный фон → проверка краёв волос на светлом/тёмном фоне → размещение на Главной Aven.

---

## 2026-09-25 — IX. 3D-направление, этап 2: цвета волос при master reference HAIR A (review)

- **Дата:** 2026-09-25
- **Задача:** Выбор владельца: HAIR A = MASTER REFERENCE Female Aven (зафиксированы лицо, форма головы, причёска, длина, пробор, выражение, взгляд, шея, плечи, одежда, композиция, фон, свет). Этап 2: 4 варианта цвета волос, менять только цвет.

### Что конкретно сделано

- Identity-locked recolor от `stage1-hair/hair-A.jpg`: COLOR A (soft black с коричневыми бликами), B (dark chocolate brown), C (chestnut, теплее B), D (dark blonde/light brown без желтизны). Оптимизация 1280px JPG (~84–92 КБ) + сравнительный лист `compare-2x2.jpg` (1600px, ~190 КБ) в `review/3d-character-concepts/stage2-color/` + README этапа.
- Обновлены статусы: `stage1-hair/README.md` (этап завершён, HAIR A = master reference), корневой README review-папки (структура этапов).
- Commit + push в `arena/01a0d8a6-aven` (PR #3 обновляется).

### Что проверено

- Визуальный контроль: во всех четырёх файлах и на сравнительном листе — одна идентичность и один кадр (лицо, глаза/цвет глаз, брови, нос, губы, кожа, макияж, выражение, причёска/длина/пробор/пряди, одежда, фон, свет, камера); отличается только цвет волос; тест «закрыть волосы — тот же кадр» выполнен. Запрещённые цвета (рыжий, синий/фиолетовый, платина, fantasy) отсутствуют.

### Что НЕ сделано (осознанно)

- Выбор цвета за владельца не делался; этап 3 (макияж) не начат; интеграции, rigged 3D, Male, правок кода нет.

### Что рекомендуется делать следующим

1. Владелец: «Беру COLOR X» → этап 3 (4 варианта макияжа).
2. После этапа 3 — полный утверждённый visual reference для pipeline настоящей 3D-модели.

---

## 2026-09-25 — VIII. 3D-направление, этап 1: причёски при зафиксированном лице (review)

- **Дата:** 2026-09-25
- **Задача:** Решение владельца: лицо Female Aven зафиксировано по последнему показанному варианту (`review/3d-character-concepts/female-D.jpg`); подобрать 4 причёски (этап 1) при неизменной идентичности; этапы 2 (цвет) и 3 (макияж) — только после выбора причёски.

### Что конкретно сделано

- Сгенерированы 4 варианта причёски с image-to-image reference conditioning от `female-D.jpg`: HAIR A (прямые до плеч), HAIR B (длинные ниже плеч, лёгкая волна), HAIR C (каре/long bob до челюсти), HAIR D (низкий мягкий пучок как у reference, современнее укладка). Цвет волос, макияж, одежда (charcoal mock-neck с violet-zip), фон, свет, камера, выражение — неизменны.
- Оптимизация: 1280px JPG (~73–76 КБ каждый) в `review/3d-character-concepts/stage1-hair/` + README этапа (статус, таблица вариантов, пригодность для realtime 3D hair cards, процесс этапов 2–3).
- Корневой README review-папки: зафиксировано решение владельца о зафиксированном лице и структура этапов 1–3.
- Commit + push в `arena/01a0d8a6-aven` (PR #3 обновляется).

### Что проверено

- Визуальный контроль всех 4 файлов: одна и та же идентичность (форма лица, глаза, брови, нос, губы, челюсть, уши, пропорции, возраст, тон кожи, выражение, взгляд), одинаковые одежда/фон/свет/кадр; различается только причёска; запрещённые элементы (косы, экстремальная длина, fantasy, огромный объём, аксессуары) отсутствуют.

### Что НЕ сделано (осознанно)

- Этапы 2 и 3 не выполнялись (ждут выбора причёски); выбор за владельца не делался; интеграции, rigged 3D, Male, правок кода Aven нет.

### Что рекомендуется делать следующим

1. Владелец: «Беру HAIR X» → этап 2 (4 цвета волос) → этап 3 (4 макияжа).
2. После завершения этапов — полный утверждённый visual reference для pipeline настоящей 3D-модели.

---

## 2026-09-25 — VII. 3D-направление: проверка ddd1c31 и visual concepts Female Aven (review)

- **Дата:** 2026-09-25
- **Задача:** Проверить, что коммит предыдущего 3D-прототипа `ddd1c31` сохранён на GitHub (и в PR #2); при успехе — подготовить visual concepts Female Aven (бюст) и остановить работу до выбора владельца.

### Важный контекст (честность)

- **`ddd1c31` НЕ найден на GitHub и НЕ сохранён удалённо:** `gh api repos/nub36/Aven/commits/ddd1c31` → 404/422 «No commit found»; commit search по репозиторию → 0; локально отсутствует даже после `git fetch --unshallow` и `fetch refs/pull/*/head`. PR #2 MERGED (merge `7d6889b`), но содержит коммиты `55387fe`, `af8e2c7`, `1c60ee4` — это 2D-аватары и голосовой UX, 3D-кода там нет.
- **В репозитории вообще нет 3D-кода:** grep по всей истории/working tree не находит Three.js, procedural head, viseme, blendshape, GLB. То есть процедурная голова, states, blink, visemes, event bus и fallback из прошлой сессии **не были push и невосстановимы** (прецедент той же природы: коммит `19b61d3`, запись VI).
- Владелец в этой сессии явно выбрал: не пересобирать 3D PoC сейчас, а перейти к visual concepts; факт утраты зафиксирован здесь, в CHARACTER.md (6.3) и CHANGELOG (V). Правило на будущее: работа считается сохранённой только после push в GitHub (+ PR); локальный коммит Arena не является сохранением.

### Что конкретно сделано

- Проверка удалённого состояния: локальный git (полная история), все refs и pull-refs, GitHub API (commit, search, branches, PR list) — результаты выше.
- Сгенерированы 4 оригинальных visual concepts Female Aven (бюст: голова + полная шея + немного плеч + верх груди; одинаковые композиция/освещение/стиль; фронт или небольшой 3/4; взгляд на пользователя): A — длинные прямые тёмно-каштановые, navy высокий ворот с violet-линией; B — волнистые auburn до плеч, indigo трикотаж; C — тёмный bob, graphite жакет поверх navy; D — низкий пучок, charcoal mock-neck с асимметричным violet-zip.
- Оптимизация для репозитория: 1280px JPG (~50–70 КБ каждый, ~240 КБ суммарно) в `review/3d-character-concepts/` + `README.md` (статус review, явное «это visual concepts, НЕ 3D models», описания кандидатов, происхождение/лицензия: оригинальная генерация проекта без сторонних ассетов, чек-лист требований к будущей 3D-модели: GLB/GLTF, rig, blendshapes, visemes+mapping, hair cards, realtime-бюджет, PBR, состояния).
- Документация: `docs/DECISIONS.md` — ADR-015 (Proposed) + строка индекса; `docs/CHARACTER.md` — раздел 6 (3D-направление, этап review, честный статус ddd1c31); `docs/CHANGELOG.md` — запись V; настоящая запись WORK_LOG.
- Commit + push в `arena/01a0d8a6-aven` + PR (см. конец записи).

### Что НЕ сделано (осознанно, по спецификации владельца)

- Победитель НЕ выбран; никакой интеграции в основной интерфейс/прототип; текущие 2D-аватары прототипа не тронуты.
- Male 3D не создавался; настоящая rigged 3D-модель не создавалась; существующая логика (event bus, states, voice, viseme abstraction, fallback) не переписывалась (её нет в репозитории — пересоздание только по отдельному решению).

### Что проверено/протестировано

- Все 4 изображения просмотрены вручную: композиция бюста, шея/плечи/ключицы, стиль, фон deep navy/indigo, отсутствие запрещённых направлений — соответствуют.
- Размеры файлов после оптимизации проверены (`identify`): 1280×~700, 50–70 КБ.
- Проверка git/GitHub-статуса воспроизводима командами из раздела контекста.

### Известные проблемы

- `ddd1c31` невосстановим (как ранее `19b61d3`): 3D PoC прошлой сессии утрачен; при потребности в Developer/Test Model — пересоздание отдельной задачей.

### Что рекомендуется делать следующим

1. Владелец смотрит `review/3d-character-concepts/` (A–D) и говорит «Выбираю вариант X» либо правки/новые кандидаты.
2. После выбора — решить pipeline настоящей модели (custom 3D / character creator / commissioned / licensed base) и лицензионную проверку; затем требования rig/blendshapes/visemes.
3. Отдельно решить судьбу Developer/Test 3D Model (пересоздание процедурного PoC) — только как dev-инструмент.
4. Невыбранные кандидаты после утверждения удалить из review-папки.

---

## 2026-09-25 — VI. Визуальный персонаж и голосовой UX в прототипе (повторная реализация)

- **Дата:** 2026-09-25
- **Задача:** Реализовать визуальных персонажей Aven (Female/Male) и голосовой UX в `/prototype`: персонаж как опциональный Presentation Layer, плавающий Aven, TTS speechSynthesis, экспериментальный STT, демо state machine (многошаговая заправка, важное событие), настройки персонажа, документация.

### Важный контекст (честность)

- Предыдущая sandbox-сессия сообщила о локальном коммите `19b61d3`, но он **недоступен и невосстановим**: его нет ни в локальном git (полная история после `fetch --unshallow`), ни в GitHub (API 404/422), ни в dangling-объектах, ни на диске. Работа **не была потеряна молча**: владелец явно разрешил повторную реализацию с нуля (решение зафиксировано здесь и в CHANGELOG). Никакой код `19b61d3` не восстанавливался — это новая реализация по спецификации.

### Что конкретно сделано

- Созданы `prototype/js/character.js` (реестр Female «Ава»/Male «Авен»/none, аватар с честным `onerror`-fallback на логотип «A», плавающий Aven с пузырём-приветствием и быстрыми действиями), `prototype/js/voice.js` (TTS `speechSynthesis` + экспериментальный STT `SpeechRecognition` с capability-детекцией и текстовым fallback), `prototype/js/flows.js` (демо state machine: «Заправка» 4 шага с валидацией и подтверждением, «Важное событие», отмена без записи), `prototype/css/character.css`.
- Сгенерированы и оптимизированы ассеты `prototype/assets/aven-female.png`, `aven-male.png` (512×512, ~290 КБ каждый, единый стиль).
- Интеграция: `index.html` (новые css/js + `#aven-float-root`), `app.js` (монтаж плавающего Aven + приветствие), `pages2.js` (Assistant: персонаж в шапке и аватары сообщений, демо-команды, 🎤 STT, авто-озвучка по настройке, `A.speak` делегирует voice.js), `settings.js` (категория «Персонаж» + честный статус TTS/STT и настройки STT), `data.js` (`settings.character`, `settings.voice.stt`).
- Документация: новый `docs/CHARACTER.md`; `docs/DECISIONS.md` (ADR-014 Proposed); `docs/VOICE.md` (раздел 10 — реализация в прототипе); `docs/UI_UX.md` (раздел 12); `docs/CHANGELOG.md` (запись IV); `prototype/README.md` (дерево файлов + раздел «Персонаж и голос»).
- `.github/workflows/prototype-pages.yml`: рабочая ветка сессии добавлялась в триггер и затем **убрана** — Pages environment protection rules разрешают деплой только `main`, деплой с рабочей ветки отклоняется («Branch … is not allowed to deploy to github-pages due to environment protection rules»). Добавлен комментарий в workflow; preview рабочей ветки — локальный сервер/sandbox, не Pages.

### Что проверено/протестировано

- `node --check` по всем 11 JS-файлам прототипа — синтаксис без ошибок.
- Headless-интеграционный тест (jsdom, вне репозитория): 33/33 PASS. Исполнены реальные кодовые пути: реестр персонажей и fallback, честная поддержка TTS/STT=false в jsdom, многошаговая заправка (валидация, подтверждение, запись в финансы/авто, отмена без записи), важное событие, рендер Assistant (демо-команды, аватар, mic-stt), монтаж/скрытие плавающего Aven, категория «Персонаж», отключение персонажа → нейтральный логотип.
- Сканирование на секреты по репозиторию — только легитимный `id-token: write` (OIDC Pages) и его упоминание в docs.

### Известные проблемы

- STT/TTS зависят от браузера; в средах без `speechSynthesis`/`SpeechRecognition` (например, headless) — честный текстовый fallback (это ожидаемо, не баг).
- Ассеты — сгенерированные иллюстрации; для production потребуется лицензирование/бренд-ревью (открытый вопрос в CHARACTER.md).
- Персонаж и голос — прототип; для production нужны отдельные ADR (ADR-014 Proposed).

### Что рекомендуется делать следующим

1. Ревью владельцем: образ персонажей, категория «Персонаж», плавающий Aven; утвердить или отклонить ADR-014.
2. Решить открытые вопросы CHARACTER.md/VOICE.md (ассеты, провайдеры STT/TTS, матрица браузеров).
3. Не переносить прототипные модули в production без ADR со статусом Accepted.

## 2026-09-25 — V. Инфраструктура preview прототипа: GitHub Pages workflow

- **Дата:** 2026-09-25
- **Задача:** Организовать постоянный визуальный preview папки /prototype через GitHub Pages (временные sandbox preview больше не используются как основной способ просмотра). Только инфраструктура UX-prototype preview — НЕ production feature.

### Что конкретно сделано

- Создан `.github/workflows/prototype-pages.yml`: checkout репозитория → загрузка **только содержимого /prototype** как Pages artifact → деплой официальными actions (checkout@v7, configure-pages@v5, upload-pages-artifact@v5, deploy-pages@v5 — версии проверены по releases на дату). permissions: `contents: read`, `pages: write`, `id-token: write`, `actions: read` (последнее требуется deploy-pages@v5). concurrency с cancel-in-progress. Триггеры: `workflow_dispatch` + автоматический `push` в явно перечисленные ветки `main` и `arena/01a0d7ec-aven` с фильтром путей `prototype/**` / workflow-файл (предположений о «main/master» не делается: ветки перечислены явно; при смене основной ветки список правится в одном месте).
- Проверена совместимость с проектным URL GitHub Pages (`https://<user>.github.io/<repo>/`): в index.html только относительные пути (css/style.css, js/*.js), навигация hash-based (`#/...`), favicon — data-URI, абсолютных путей нет. index.html попадает в корень артефакта, поэтому вложенность подкаталога не ломает загрузку.
- Workflow закоммичен и запушен в `origin/arena/01a0d7ec-aven`; GitHub зарегистрировал workflow (Actions → «Prototype Pages», active).
- Попытка включить Pages через API (`gh api repos/nub36/Aven/pages`): GET → 404 (не включён), POST → 403 «Resource not accessible by integration» — у токена агента нет прав администрирования Pages. Обход прав не предпринимался; включение Source: GitHub Actions — шаг владельца (инструкция добавлена).
- `prototype/README.md` дополнен разделом «GitHub Pages preview»: пошагово Settings → Pages → Source: GitHub Actions → запуск workflow → где взять ссылку; ожидаемый URL `https://nub36.github.io/Aven/`; правила обновления; замечание о приватных репозиториях.
- В CHANGELOG не записывалось (не продуктовая фича), production-архитектура/ADR не менялись, prototype не переделывался, зависимости не добавлялись.

### Какие файлы изменены

- Создано: `.github/workflows/prototype-pages.yml`
- Обновлено: `prototype/README.md`, `docs/WORK_LOG.md` (эта запись)

### Что проверено/протестировано

- YAML синтаксис; наличие workflow на GitHub (`gh workflow list` → Prototype Pages, active).
- Отсутствие абсолютных путей в prototype (grep); относительные ресурсы и hash-навигация — совместимо с подкаталогом Pages.
- Права: включение Pages владельцем — подтверждено 403 от API (ожидаемо).

### Известные проблемы

- Preview не активен, пока владелец не выберет Source: GitHub Actions в Settings → Pages и не запустит workflow (первый деплой).
- Если репозиторий приватный — Pages может требовать платный план GitHub.

### Что рекомендуется делать следующим

1. Владельцу: Settings → Pages → Source: GitHub Actions → Actions → Prototype Pages → Run workflow (ветка arena/01a0d7ec-aven или main).
2. Проверить постоянный URL: https://nub36.github.io/Aven/
3. Далее — по плану: вопросы №31–35 и утверждение ADR стека (прототип остаётся UX-справочником).

## 2026-09-25 — IV. Визуальный интерактивный прототип (/prototype)

- **Дата:** 2026-09-25
- **Задача:** Создать отдельный визуальный кликабельный прототип будущего сайта Aven для UX/продуктового проектирования. НЕ production: без backend, БД, авторизации, API, миграций, реального STT/TTS.

### Что конкретно сделано

- Выбрана максимально простая технология: **статический HTML/CSS/JS без зависимостей и сборки** (классические скрипты, работает с `file://`). Технология прототипа **не является ADR** для production; Proposed-ADR не менялись.
- Создана структура: `prototype/index.html` (оболочка: сайдбар, топбар, рабочая область), `css/style.css` (единый дизайн: spacing/buttons/forms/modal/cards/typography, light+dark темы, базовый адаптив), `js/` (data/state/ui/pages1/pages2/tools/settings/app).
- Реализованы страницы (hash-роутинг, реальное переключение): Главная (приветствие по времени суток, демо-дата, командная строка + кнопка микрофона → «Голосовой ввод — прототип», следующее событие «Стоматолог 10:00 через 1 ч 24 мин», карточки Сегодня/Задачи/Расходы/Автомобиль/Быстрые действия), День (Вчера/Сегодня/Завтра, timeline, задачи, напоминания, выполненное), Календарь месяца (demo-события, карточка события, +Событие, переключение месяцев), Задачи (фильтры Все/Сегодня/Предстоящие/Выполненные, рабочие checkbox, форма), Заметки (список/содержимое, поиск, теги, закрепление), Финансы (баланс/месяц, CSS-график, категории, операции, +Операция), Авто (карточка BMW 530d, вкладки Обзор/Заправки/Расходы/Обслуживание/Документы/История, +Заправка/+Расход/+Обслуживание/Обновить пробег), Покупки (карточки с гарантиями: активна/истекает/скоро), Инструменты (13 карточек; рабочие: проценты, разница дат, конвертер, топливо, поездка, пароль, Base64, URL, Unix time, UUID, JSON, Diff; QR — демо-изображение), Aven Assistant (полноэкранный режим, демо-диалог по спецификации, suggestions, кнопка 🔊 — browser speechSynthesis с учётом настроек, иначе визуальная реакция), Автоматизации (вкл/выкл, последний/следующий запуск, +Автоматизация, макет «Visual Automation Canvas — планируется» с превью [08:00]→[Получить события]→[Произнести]), Настройки (17 категорий: Профиль, Aven, Голос, Уведомления, Команды, Словарь, Память, Главная, Модули, Автоматизации, Интеграции, Синхронизация, Файлы, Приватность, Безопасность, Доступность, Экспериментальные), Профиль.
- Интерактивность: модальные окна (Esc/overlay/X), вкладки, checkbox, переключатели, слайдеры, выбор настроек, смена темы (топбар и настройки), размер текста, reduced motion, экспорт состояния (JSON), сброс демо-данных. Состояние — JS memory + localStorage (`aven-proto-v1`). Тогглы «Модули» реально скрывают пункты меню, «Главная» — карточки главной. Опасные действия в демо-формах идут через confirm-модалку (по духу ADR-005).
- Документация: README.md — раздел «Visual Prototype» (дисклеймер: только UX, не production-архитектура); UI_UX.md — ссылка на /prototype; PROJECT_PLAN.md — строка в карте документации + новые открытые вопросы №32–35 (дефолтная тема; пересечение командной строки главной и Assistant; адаптивность; Профиль — отдельный пункт меню vs часть настроек). В CHANGELOG **не** записывалось как продуктовая фича (по указанию).
- Production-код, backend, auth, БД, миграции, зависимости — НЕ создавались. Менеджер пакетов не использовался.

### Какие файлы изменены

- Создано: `prototype/index.html`, `prototype/README.md`, `prototype/css/style.css`, `prototype/js/{data,state,ui,pages1,pages2,tools,settings,app}.js`
- Обновлено: `README.md`, `docs/UI_UX.md`, `docs/PROJECT_PLAN.md`, `docs/WORK_LOG.md` (эта запись)

### Что проверено/протестировано

- `node --check` — синтаксис всех 8 JS-файлов.
- Смоук-тест в Node (заглушка DOM): рендер всех 13 страниц, 13 экранов инструментов, 17 категорий настроек — ALL PASS; найдены и исправлены 2 бага (несогласованный API состояния `S.s` в pages2/settings; profile возвращал строку вместо {html}).
- Статический сервер поднят (0.0.0.0:8080), `curl`: index/css/js — 200.

### Известные проблемы

- QR-код — декоративное демо-изображение (не сканируется); микрофон и настройка «звук перед голосом» — заглушки; Assistant отвечает по скрипту (ключевые слова), реальный Command Engine — Stage 2.
- Полный перезапуск браузера не тестировался (ручная проверка владельцем ожидается); специфичные браузеры (Safari/Firefox) для speechSynthesis — проверит владелец.

### Что рекомендуется делать следующим

1. Владельцу: открыть прототип, пройтись по страницам; фиксировать правки UX (прототип можно свободно переделывать вместе с планом).
2. Решить новые открытые вопросы №32–35; затем вернуться к вопросу №31 (состав Stage 1).
3. После утверждения состава Stage 1 и ADR стека — начинать production-разработку (прототип остаётся справочником UX, не кодовой базой).

## 2026-09-25 — III. Направление Web-first: порядок Stage 1–6, разделы Web Aven, реприоритизация

- **Дата:** 2026-09-25
- **Задача:** По решению владельца зафиксировать текущий приоритет разработки **WEB-FIRST** (не необратимое архитектурное решение); обновить план, состав web-разделов, трактовку offline, перенос mobile/desktop в FUTURE; не начинать production-разработку.

### Что конкретно сделано

- **DECISIONS.md**: добавлен **ADR-013 «Web-first»** со статусом `Product Direction` (может быть пересмотрено владельцем); добавлен статус `Product Direction` в таблицу статусов; усилено разграничение `Product Principle`/`Product Direction` ≠ `Proposed`/`Under Discussion` ≠ `Accepted`; явная пометка, что ADR-001…010 не утверждались владельцем автоматически; в ADR-007 добавлено уточнение приоритета (offline-синхронизация не условие web-запуска).
- **PROJECT_PLAN.md** переработан: баннер с направлением Web-first; раздел 2 — диаграмма единого пути действия (UI/Forms + Text + Voice → Action Core → Business Logic → Data; Result → UI/Text/TTS); раздел 4 — новый порядок **Stage 0–6** (1: полезный самостоятельный сайт; 2: Command Engine; 3: голос поверх CE; 4: автоматизации/Canvas; 5: оценка мобильного переноса по реальному использованию; 6: Android/iOS и другие клиенты) с пометкой «продуктовый приоритет, все Stage могут пересматриваться»; раздел 5 — MVP-черновик пересмотрен (голос вынесен в Stage 3, добавлены День/Календарь/Заметки/Покупки/Инструменты); **новый раздел 6 «Web-first: принцип и состав Web Aven»** (основной принцип — не оболочка мобильного/не только голос/не только чат/не пустой dashboard/не демонстрация технологий; обязательное UX-правило «полезен без голоса»; структура из 12 разделов; краткие описания; автоматизации раннего этапа; mobile/desktop → FUTURE; offline-трактовка; Web-first ≠ Web-only); MVP-состав и открытые вопросы обновлены (новые №27–31); разделы перенумерованы (настройки — 11, управление настройками — 12, user/admin — 13, правила — 14).
- **FEATURES.md** переработан: новый раздел 2 «Разделы Web Aven» с полным составом 12 разделов (Главная — персональный центр с настраиваемыми карточками; День; Календарь; Задачи; Заметки; Финансы через формы; Авто с примером «один Action/Use Case» для UI/Text/Voice; Покупки/Имущество со статусом собственности; **Инструменты** — полный перечень утилит + локальная обработка приватных файлов; **Aven Assistant** — отдельный режим, не заменяет страницы; Автоматизации; Настройки); маркеры Stage; раздел 3 обновлён (текстовые команды — Stage 2, голос — Stage 3).
- **UI_UX.md**: правило «сайт полезен без голоса» (1.1); разделы сайта + Assistant + Инструменты (1.2); Главная дополнена (дата, сегодняшний план, показатели, настраиваемые карточки, микрофон «когда voice доступен»).
- **ARCHITECTURE.md**: принципы Web-first; схема 3.1 единого пути действия; Action Core — UI/Forms как первоклассный источник; голос — слой поверх CE (Stage 3); автоматизации — Stage 4; offline — перспектива; раздел 13 — Web-first ≠ Web-only, доверенные клиенты без искусственных препятствий.
- **COMMAND_ENGINE.md**: привязка к Stage 2 («подключается к уже существующим функциям сайта»; те же действия через UI); примеры команд.
- **VOICE.md**: голос — слой поверх текстового Command Engine; диаграмма `audio → STT → text → CE` / `result → response → optional TTS`; правило «ни одна базовая функция не зависит от микрофона/STT/TTS».
- **AUTOMATION.md**: Canvas — Stage 4; ранний этап (готовые автоматизации, простые правила, расписания); Canvas после стабилизации данных и Action Core; срок не утверждён.
- **OFFLINE_SYNC.md**: статус **Future / Under Design** (документ сохранён); текущая трактовка — сложная offline-sync не условие web-запуска; локальные возможности браузера — где практично.
- **FUTURE.md**: принцип переноса («сначала полезный Web → наблюдать → определять набор Android/iOS»); явные списки «не реализовывать сейчас» (mobile: offline DB, background voice, постоянное прослушивание, сложная device sync; desktop: запуск программ, управление ОС, ФС, shutdown, monitoring).
- **AGENT_GUIDE.md**: правило 7 (Web-first, порядок Stage), правило 8 (различение статусов решений).
- **README.md**: текущее направление Web-first, статусы решений.
- **INTEGRATIONS.md**: добавлен курс валют (внешний источник для инструмента «валюты»).
- **Связанные правки**: перенумерация ссылок на разделы FEATURES (3.1→4.1, 3.2→4.2, 5→6, 6→7, 8→9) и PROJECT_PLAN (11→12, 12→13) в VOICE, DATA_MODEL, SECURITY, FUTURE, ARCHITECTURE, COMMAND_ENGINE, UI_UX, ADMIN.
- Production-код НЕ создавался, frontend/backend НЕ создавались, зависимости НЕ устанавливались, database migrations НЕ создавались.

### Какие файлы изменены

- `docs/DECISIONS.md`, `docs/PROJECT_PLAN.md` (переработаны), `docs/FEATURES.md` (переработан)
- `docs/ARCHITECTURE.md`, `docs/UI_UX.md`, `docs/COMMAND_ENGINE.md`, `docs/VOICE.md`, `docs/AUTOMATION.md`, `docs/OFFLINE_SYNC.md`, `docs/FUTURE.md`, `docs/AGENT_GUIDE.md`, `docs/INTEGRATIONS.md`
- `README.md`, `docs/WORK_LOG.md` (эта запись), `docs/CHANGELOG.md`

### Что проверено/протестировано

- Проверены перенумерованные перекрёстные ссылки (FEATURES и PROJECT_PLAN разделы) во всех документах.
- Проверено, что Stage-порядок согласован между PROJECT_PLAN (раздел 4), FEATURES (маркеры), DECISIONS (ADR-013), AGENT_GUIDE (правило 7), AUTOMATION/VOICE/COMMAND_ENGINE (Stage-пометки).
- Проверено отсутствие кода/зависимостей/migrations в репозитории.

### Известные проблемы

- Состав Stage 1 / MVP (какие из 12 разделов входят) — не утверждён (открытый вопрос №31).
- Набор инструментов первого релиза и источник курсов валют — не определены (№27, №28).
- Дефолтный набор карточек главной (№29), границы простых правил автоматизаций (№30) — открыты.
- Все технические решения остаются `Proposed`/`Under Discussion`; ADR-013 — `Product Direction` (пересматриваемо).

### Что рекомендуется делать следующим

1. Владельцу: утвердить состав Stage 1 (какие разделы входят в первую web-версию) и базовый набор Инструментов.
2. Владельцу: перевести ключевые ADR (стек, ADR-004) в `Accepted` перед началом Stage 1.
3. Затем — разработка Stage 1 строго по утверждённому составу.

## 2026-09-25 — Пересмотр статусов ADR; изменяемость архитектуры; система настроек

- **Дата:** 2026-09-25
- **Задача:** По прямому указанию владельца: (1) пересмотреть статусы ADR-001…010 — владелец НЕ утверждал их как неизменяемые архитектурные решения; (2) зафиксировать правило изменяемости архитектуры; (3) добавить систему пользовательских настроек (категории, управление настройками через команды, разделение user settings и admin).

### Что конкретно сделано

- **DECISIONS.md** переработан:
  - введены статусы `Product Principle` / `Proposed` / `Under Discussion` / `Accepted` / `Rejected` / `Superseded` (вместо «Принято»/«Предложено»/«Заменено»);
  - ADR-001…010 пересмотрены: продуктовые принципы, явно заданные владельцем, получили `Product Principle` (ADR-001, 002, 003, 005, 006, 007, 008, 009, 010); архитектурный подход ADR-004 переведён в `Proposed`;
  - добавлено явное правило: «До начала production-разработки архитектура Aven находится в стадии проектирования. Любое Proposed/Under Discussion решение может быть изменено владельцем проекта без необходимости сохранять обратную совместимость с ещё не существующей production-реализацией» + правило о документируемых изменениях после начала разработки + «ADR не трактуется как запрет на изменение архитектуры»;
  - добавлены новые ADR: ADR-011 (настройки через команды, `settings.*` actions, security-ограничения) и ADR-012 (разделение `/settings` и `/admin`, запрет автоматического действия от имени пользователя, требования к будущему impersonation) — оба `Product Principle`;
  - технологические ADR-101…108 переведены на новую лексику статусов (`Proposed`/`Under Discussion`).
- **AGENT_GUIDE.md** дополнен: раздел «Изменяемость документации и приоритет источников требований» (документация — source of truth текущего состояния, но не неизменяемая; владелец может менять требования/архитектуру/MVP/технологии/приоритеты; нельзя отказываться от нового требования из-за старого ADR; при конфликте приоритет у нового решения владельца; конфликт документируется; старое решение помечается `Superseded`/`Rejected`); добавлен приоритет источников требований (1. Последнее прямое решение владельца → 2. PROJECT_PLAN → 3. Product Principles → 4. ADR → 5. остальная документация → 6. предположения агента); правило «при серьёзном противоречии — вынести владельцу, не решать самостоятельно».
- **PROJECT_PLAN.md**:
  - добавлен раздел «9. Статус архитектуры» (архитектура проектируется; production stack не утверждён; границы MVP не утверждены окончательно; Canvas/mobile/desktop/offline-sync — предметы планирования/проектирования; AI — необязательное направление; документация развивается с концепцией);
  - добавлен раздел «10. Настройки Aven» — 17 категорий пользовательских настроек (Профиль, Голос, Поведение Aven, Голосовые события/уведомления, Команды, Персональный словарь, Память, Автоматизации, Модули, Главная, Приватность, Безопасность, Интеграции, Синхронизация, Файлы, Доступность, Экспериментальные функции) с полным составом каждой;
  - добавлен раздел «11. Управление настройками через Aven» (примеры фраз, `settings.*` actions, запрет обхода security, подтверждение/реаутентификация для чувствительных);
  - добавлен раздел «12. Разделение User Settings и Admin» (`/settings` vs `/admin`, правила будущего support impersonation);
  - раздел «Правила изменения плана» стал разделом 13 и дополнен приоритетом источников требований и правилом изменяемости архитектуры;
  - открытые вопросы расширены с 18 до 26 (№19–26: чувствительные настройки, охват settings-команд, механизм реаутентификации, платформозависимые голосовые функции, состав настроек MVP, диагностические данные, impersonation, настройки главной/доступности/экспериментальных функций).
- **Связанные документы обновлены:** UI_UX.md (новый раздел «Настройки в интерфейсе (/settings)»), COMMAND_ENGINE.md (settings-intents, security-примечание), VOICE.md (таксономия голосовых настроек по категориям, тест голоса, fallback), SECURITY.md (новый раздел «Настройки и разделение /settings vs /admin», impersonation-требования), ADMIN.md (разделение `/settings`/`/admin` в ограничениях), ARCHITECTURE.md (settings-actions через Action Core, статус архитектуры), FEATURES.md (новый подраздел 2.8 «Настройки»), DATA_MODEL.md (профиль расширен: регион, форматы даты/времени, начало недели; управление памятью через настройки), OFFLINE_SYNC.md (уточнён статус офлайн-стратегии — Under Discussion).
- Production-код НЕ создавался, зависимости НЕ устанавливались, реализация НЕ начиналась.

### Какие файлы изменены

- `docs/DECISIONS.md` (переработан), `docs/AGENT_GUIDE.md` (переработан), `docs/PROJECT_PLAN.md`
- `docs/UI_UX.md`, `docs/COMMAND_ENGINE.md`, `docs/VOICE.md`, `docs/SECURITY.md`, `docs/ADMIN.md`, `docs/ARCHITECTURE.md`, `docs/FEATURES.md`, `docs/DATA_MODEL.md`, `docs/OFFLINE_SYNC.md`, `docs/FUTURE.md`
- `docs/WORK_LOG.md` (эта запись + пометка к записи выше), `docs/CHANGELOG.md`

### Что проверено/протестировано

- Проверено отсутствие устаревших статусных слов («Принято»/«Предложено» как статусы ADR) во всех документах, кроме исторических записей WORK_LOG и определения статусов в DECISIONS.md.
- Проверена согласованность: приоритет источников в AGENT_GUIDE == PROJECT_PLAN раздел 13; ADR-011/012 упоминаются в COMMAND_ENGINE, SECURITY, ADMIN, ARCHITECTURE, FEATURES, UI_UX; settings-интенты совпадают с PROJECT_PLAN раздел 11.
- Относительные ссылки в изменённых файлах проверены (файлы существуют).

### Известные проблемы

- Перечень «чувствительных настроек» и охват настроек, изменяемых командой, не определены (открытые вопросы №19, №20).
- Все решения остаются в статусах `Product Principle`/`Proposed`/`Under Discussion`; статус `Accepted` не присвоен ни одному ADR — ни одно архитектурное/технологическое решение пока не утверждено владельцем окончательно.

### Что рекомендуется делать следующим

1. Владельцу: подтвердить (или скорректировать) список Product Principles и при желании перевести ключевые ADR (например, ADR-004) в `Accepted`.
2. Определить перечень чувствительных настроек и охват `settings.*` actions (вопросы №19–20).
3. Продолжить работу по этапу 1 (ответы на открытые вопросы, ADR по стеку).

## 2026-09-25 — Создание документационной основы проекта

- **Дата:** 2026-09-25
- **Задача:** Создать документационную основу проекта Aven и зафиксировать концепцию (этап 0; постановка — «Задача агента сейчас»).

> ⚠️ **Примечание (внесено позже тем же числом):** формулировка «10 принятых решений» в этой записи устарела — владелец НЕ утверждал ADR-001…010 как неизменяемые архитектурные решения. Статусы пересмотрены: продуктовые принципы → `Product Principle`, остальное → `Proposed`/`Under Discussion`. См. запись ниже.

### Что конкретно сделано

- Создана структура `/docs` из 17 документов + переработан корневой `README.md`.
- Спецификация концепции распределена по профильным документам без потери деталей:
  - концепция, принципы, цель, примеры — README, PROJECT_PLAN, ARCHITECTURE;
  - главный интерфейс, утро/вечер, UX-принцип, suggestions, история действий, честность статусов в UI — UI_UX;
  - голос: архитектура, веб-MVP, настройки, ограничения платформ — VOICE;
  - напоминания/события (типы, важность), память Aven, модули, авто, финансы, прочие данные, универсальные сущности — FEATURES, DATA_MODEL;
  - Command Engine: intents/actions, конвейер, естественная речь без AI, пользовательские команды, персональный словарь, режим тестирования, confidence, шаблонные ответы — COMMAND_ENGINE;
  - Automation Canvas, типы блоков, запрет пользовательского JS на сервере, готовые шаблоны — AUTOMATION;
  - offline-first, состояния, синхронизация — OFFLINE_SYNC;
  - безопасность и приватность — SECURITY;
  - полная спецификация админ-панели (все 21 раздел) — ADMIN;
  - будущие приложения (mobile, desktop-agent), AI-слой — FUTURE, ARCHITECTURE;
  - Action Core (источники команд, стандартные действия) — ARCHITECTURE;
  - интеграции (что известно из концепции) — INTEGRATIONS;
  - технологический вектор — PROJECT_PLAN (раздел 7) + DECISIONS (ADR-101…108, статус «Предложено/Открыто»).
- Создан начальный реестр решений DECISIONS.md: 10 принятых решений (зафиксированы концепцией: docs-first, ядро без AI, текстовое представление команд, intent→action, подтверждение опасных действий, запрет произвольного JS, offline-first, privacy/security by design, модульность, честность статусов) и 8 предложенных/открытых (стеки, БД, очередь, кэш, хранилище) — предположения НЕ выданы за утверждённые решения.
- Зафиксированы 18 открытых вопросов в PROJECT_PLAN.md (раздел «Открытые вопросы»), включая явные развилки из концепции (Canvas в MVP или нет; пороги confidence; очередь задач; кэш «если обосновано»).
- Созданы CHANGELOG.md (состояние «Project planning / no production release») и AGENT_GUIDE.md (правила работы агентов).
- Приложение НЕ разрабатывалось, зависимости НЕ устанавливались, production-код НЕ создавался.

### Какие файлы изменены

- `README.md` (переписан)
- `docs/PROJECT_PLAN.md`
- `docs/AGENT_GUIDE.md`
- `docs/DECISIONS.md`
- `docs/ARCHITECTURE.md`
- `docs/FEATURES.md`
- `docs/COMMAND_ENGINE.md`
- `docs/VOICE.md`
- `docs/AUTOMATION.md`
- `docs/DATA_MODEL.md`
- `docs/OFFLINE_SYNC.md`
- `docs/SECURITY.md`
- `docs/ADMIN.md`
- `docs/UI_UX.md`
- `docs/INTEGRATIONS.md`
- `docs/FUTURE.md`
- `docs/CHANGELOG.md`
- `docs/WORK_LOG.md` (этот файл)

### Что проверено/протестировано

- Проверено наличие всех 18 требуемых файлов (README + 17 в /docs) и корректность заголовков/навигационных ссылок между документами.
- Проверено, что все пункты исходной спецификации распределены по документам (пройден по разделам 1–36 постановки).
- Проверено отсутствие production-кода и файлов зависимостей в репозитории (только документация).

### Известные проблемы

- Противоречий в исходной спецификации не обнаружено; зафиксированы развилки/неопределённости — 18 открытых вопросов в PROJECT_PLAN.md (раздел 8).
- Конкретные значения по умолчанию (пороги confidence, границы «утро/вечер», провайдеры, стек) пока не определены — это ожидаемо на этапе 0.

### Что рекомендуется делать следующим

1. Ревью документации владельцем проекта; утвердить или скорректировать принятые ADR-001…010.
2. Ответить на ключевые открытые вопросы PROJECT_PLAN.md (раздел 8): состав MVP, Canvas в MVP или нет, стек (ADR-101…108), стратегия конфликтов синхронизации.
3. Утвердить финальный состав MVP → перевести проект на этап 1 (ADR) / затем этап 2 (реализация вертикального среза).
4. Не начинать код до фиксации соответствующих решений со статусом «Принято» в DECISIONS.md.
