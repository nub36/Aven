# DECISIONS — Решения проекта: продуктовые принципы и ADR

> **Статус:** реестр решений проекта. Источник истины по тому, что зафиксировано и в каком статусе.
> **Последнее обновление:** 2026-09-26
>
> **Важно:** владелец проекта **не** утверждал ADR-001…010 как неизменяемые архитектурные решения (и не утверждал их автоматически). Часть из них — продуктовые принципы, заданные владельцем (статус `Product Principle`); они фиксируют **цель/ограничение продукта**, но **не** способ реализации. Никакое архитектурное или технологическое решение сейчас не является необратимым.
>
> **Разграничение понятий:** `Product Principle` / `Product Direction` ≠ `Proposed Architecture Decision`. Принцип/направление фиксирует цель; **способ реализации** фиксируется только ADR со статусом `Accepted`. Все не утверждённые владельцем технические решения имеют статус `Proposed` или `Under Discussion`.

---

## Статусы

| Статус | Смысл |
|---|---|
| `Product Principle` | Продуктовый принцип, явно заданный владельцем. **НЕ** окончательное архитектурное решение: способ реализации требует отдельного ADR со статусом `Accepted` до production-разработки |
| `Product Direction` | Текущее продуктовое направление, заданное владельцем. **НЕ** архитектурное решение; **может быть пересмотрено владельцем проекта** |
| `Proposed` | Предложено; требует утверждения владельцем |
| `Under Discussion` | Активно обсуждается; решения нет (на 2026-09-26 таких ADR нет: ADR-106 и ADR-107 переведены в `Proposed` — подготовлены сравнение альтернатив и обоснование, решение за владельцем) |
| `Accepted` | Утверждено владельцем (пока нет ни одного) |
| `Rejected` | Отклонено |
| `Superseded` | Заменено другим ADR (указан замещающий ID) |

## Правила изменяемости архитектуры

> **«До начала production-разработки архитектура Aven находится в стадии проектирования. Любое Proposed/Under Discussion решение может быть изменено владельцем проекта без необходимости сохранять обратную совместимость с ещё не существующей production-реализацией.»**

- После начала разработки архитектура также может изменяться, но изменение должно документироваться **новым ADR с причиной и последствиями**.
- ADR **не трактуется** как запрет на изменение архитектуры.
- Старое решение **не удаляется бесследно**: при реальной замене получает статус `Superseded`/`Rejected` со ссылкой на новое.
- При конфликте **нового прямого решения владельца** со старым ADR **приоритет имеет решение владельца**; конфликт документируется (порядок приоритетов источников требований — [AGENT_GUIDE.md](AGENT_GUIDE.md)).
- Реализация соответствующей части системы не начинается, пока **способ реализации** не зафиксирован ADR со статусом `Accepted`.

---

## Индекс

| ID | Статус | Кратко |
|---|---|---|
| [ADR-001](#adr-001) | Product Principle | Документация — источник истины **текущего состояния**; docs-first процесс; документация не неизменяема |
| [ADR-002](#adr-002) | Product Principle | Основные функции работают без обязательного генеративного AI; AI — опциональный слой |
| [ADR-003](#adr-003) | Product Principle | Текстовый ввод всегда доступен; внутренний формат команды — текст; ответы текст + озвучка |
| [ADR-004](#adr-004) | Proposed | Архитектурный подход: intents/actions + Action Core, без дублирования бизнес-логики |
| [ADR-005](#adr-005) | Product Principle | Опасные действия требуют подтверждения; история действий и Undo; пороги confidence — открыты |
| [ADR-006](#adr-006) | Product Principle | Запрет произвольного пользовательского JavaScript на production-сервере (принцип безопасности) |
| [ADR-007](#adr-007) | Product Principle | Частичная работа без интернета — требование продукта; офлайн-стратегия — Under Discussion |
| [ADR-008](#adr-008) | Product Principle | Пользователь контролирует свои данные; privacy/security by design как принцип |
| [ADR-009](#adr-009) | Product Principle | Модульность и расширяемость, включая будущие пользовательские типы сущностей |
| [ADR-010](#adr-010) | Product Principle | Честность статусов выполнения (не изображать выполненное) |
| [ADR-011](#adr-011) | Product Principle | Настройки доступны не только через UI, но и текстовыми/голосовыми командами через стандартные Actions |
| [ADR-012](#adr-012) | Product Principle | Разделение `/settings` (пользователь) и `/admin` (система); админ не действует от имени пользователя автоматически |
| [ADR-013](#adr-013) | Product Direction | **Web-first**: первая полноценная реализация — самостоятельный полезный веб-сайт; порядок Stage 1–6. Может быть пересмотрено владельцем |
| [ADR-014](#adr-014) | Proposed | Персонаж Aven (Female/Male) — **опциональный Presentation Layer**; бизнес-логика и команды не зависят от образа. Реализовано в прототипе; требует утверждения для production |
| [ADR-015](#adr-015) | Proposed | 3D Aven — формат **бюста** (голова + шея + плечи + верх груди), сначала только Female; текущий этап — visual concepts, интеграция только после явного выбора владельца |
| [ADR-101](#adr-101) | Proposed | Backend: TypeScript + Node.js 24 LTS; фреймворк — NestJS **или** Fastify (альтернативы сравнены) |
| [ADR-102](#adr-102) | Proposed | Server DB: PostgreSQL 17/18 |
| [ADR-103](#adr-103) | Proposed | Web: React + TypeScript; развилка «отдельный API + SPA» / «full-stack Next.js» |
| [ADR-104](#adr-104) | Proposed | Mobile (будущее): Flutter/Dart, локальная БД SQLite |
| [ADR-105](#adr-105) | Proposed | Desktop-agent (будущее): Rust + Tauri |
| [ADR-106](#adr-106) | Proposed | Background jobs: pg-boss (очередь в PostgreSQL) с критериями перехода на BullMQ + Valkey |
| [ADR-107](#adr-107) | Proposed | Кэширование: отдельный кэш-сервер на Stage 1 **не вводится**; критерии введения зафиксированы (Valkey, не Redis 8) |
| [ADR-108](#adr-108) | Proposed | Object storage: абстракция `StorageProvider`, целевой контракт S3-совместимый; MinIO **не** выбирать (репозиторий архивирован) |
| [ADR-109](#adr-109) | Proposed | Доступ к данным и миграции: Drizzle ORM + drizzle-kit (альтернативы Prisma 7, Kysely) |
| [ADR-110](#adr-110) | Proposed | Аутентификация/сессии/2FA: Better Auth (встроенная библиотека) + свой RBAC и аудит |
| [ADR-111](#adr-111) | Proposed | Контракт API и валидация: REST + OpenAPI, общие Zod-схемы клиента и сервера |
| [ADR-112](#adr-112) | Proposed | Тестирование и качество: Vitest + Testing Library + Playwright + интеграционные на реальном PostgreSQL |
| [ADR-113](#adr-113) | Proposed | Репозиторий и деплой: монорепо pnpm workspaces, Docker Compose на VPS, GitHub Actions, бэкап с проверенным восстановлением |

---

## Продуктовые принципы (Product Principle)

> Принципы явно заданы владельцем в концепции/инструкциях. Они **не являются окончательными архитектурными решениями**; механизмы реализации требуют отдельных ADR (`Accepted`) до production-разработки.

### ADR-001

- **ID:** ADR-001
- **Дата:** 2026-09-25 (уточнён 2026-09-25)
- **Статус:** Product Principle
- **Решение:** Документация в репозитории — **source of truth текущего состояния проекта**; при этом документация **не является неизменяемой**: владелец может менять требования, архитектуру, MVP, технологии и приоритеты. Вводится docs-first процесс: перед существенной работой читать PROJECT_PLAN → AGENT_GUIDE → профильные документы → DECISIONS; после задачи обновлять WORK_LOG и профильные документы. Приоритет источников требований — см. [AGENT_GUIDE.md](AGENT_GUIDE.md).
- **Причина:** Владелец явно объявил документацию источником истины; при этом проект в стадии проектирования, и документы должны отражать актуальные решения владельца, а не «замораживать» старые.
- **Последствия:** Любые изменения проекта сопровождаются обновлением документов; накладные расходы на ведение документации; обязательный журнал работ и реестр решений; устаревшие решения помечаются `Superseded`/`Rejected`, а не удаляются.

### ADR-002

- **ID:** ADR-002
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** Основные возможности Aven работают **без** обязательного генеративного AI. AI в будущем — дополнительный слой (language parser, intent router, помощник по сложным запросам), необязательный для базовой работы. При отсутствии AI или интернета основные функции продолжают работать. AI не имеет неконтролируемого прямого доступа к БД и системным операциям: `Human input → AI Router (опционально) → строго определённый Action + parameters → permission validation → execution`.
- **Причина:** Принцип явно задан владельцем: Aven должен уметь работать без платного LLM API; зависимость ядра от внешнего платного провайдера недопустима.
- **Последствия:** Command Engine проектируется на детерминированных методах (шаблоны, словари, морфология, entity resolution); AI-слой — подключаемая надстройка со строгим контрактом. Конкретный AI-провайдер/модель — будущее отдельное решение.

### ADR-003

- **ID:** ADR-003
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **Текстовый ввод всегда доступен** наряду с голосовым; микрофон не обязателен. Внутренняя система работает с **текстовым представлением команды** (голос превращается в текст до входа в движок). Ответы могут одновременно показываться текстом и озвучиваться.
- **Причина:** Принцип явно задан владельцем.
- **Последствия:** Intents/actions/парсеры определяются над текстом; в UI всегда есть текстовый ввод и текстовый ответ. Заменяемость голосового слоя (STT/TTS за интерфейсами) — **архитектурное направление в статусе Proposed**, реализация требует отдельного ADR.

### ADR-004

- **ID:** ADR-004
- **Дата:** 2026-09-25 (пересмотрен 2026-09-25: понижен до Proposed)
- **Статус:** Proposed
- **Решение:** Архитектурный подход: ядро обработки — **Command Engine на intents/actions** + **Action Core**: любой источник команды (UI, текст, голос, Automation, API, в будущем AI) превращается в стандартное действие (например, `ADD_EXPENSE`, `CREATE_REMINDER`); бизнес-логика не дублируется для голоса и UI.
- **Причина:** Концепция задаёт направление («внутренне использовать intents/actions»), но конкретная архитектура ядра — проектное решение, не утверждённое владельцем как окончательное.
- **Последствия:** Требуется утверждение владельцем (возможны альтернативы/уточнения контракта действий); до `Accepted` реализация ядра не начинается.

### ADR-005

- **ID:** ADR-005
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **Опасные/разрушительные действия требуют подтверждения** независимо от confidence. Вводится оценка уверенности распознавания (числовые пороги НЕ утверждены): высокая + безопасное → выполнить; средняя → уточнить/подтвердить; низкая → переформулировать. Ведётся история действий; для подходящих действий — Undo. Недостающие параметры запрашиваются уточнением; опасные/существенные данные не угадываются.
- **Причина:** Принцип явно задан владельцем.
- **Последствия:** Классификация действий по опасности и конкретные пороги confidence — отдельные будущие решения; хранится журнал действий с Undo для подходящих действий.

### ADR-006

- **ID:** ADR-006
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **На production-сервере запрещено выполнять произвольный пользовательский JavaScript** (в т.ч. в Automation Canvas). Направление реализации: каждый серверный блок автоматизации — заранее зарегистрированная операция с входами, выходами, permissions, validation, resource limits.
- **Причина:** Принцип безопасности, явно заданный владельцем («НЕЛЬЗЯ по умолчанию»).
- **Последствия:** Конкретный механизм реестра блоков, модель permissions и resource limits — **Proposed**, требуют отдельного ADR; произвольный пользовательский JS на сервере не исполняется независимо от выбора механизма.

### ADR-007

- **ID:** ADR-007
- **Дата:** 2026-09-25 (пересмотрен 2026-09-25)
- **Статус:** Product Principle
- **Решение:** **Продуктовое требование (явно задано владельцем): система должна иметь возможность работать частично без интернета.** Архитектурная стратегия обеспечения этого требования (offline-first/local-first, выбор локальных хранилищ, синхронизация, разрешение конфликтов, статусы Online/Offline/Sync pending/Local-private data) — **Under Discussion** (см. [OFFLINE_SYNC.md](OFFLINE_SYNC.md)).
- **Причина:** Требование зафиксировано в концепции; способ его архитектурной реализации владельцем не утверждён.
- **Последствия:** Клиенты проектируются с учётом офлайн-требования; стратегия синхронизации и хранилищ — отдельные решения. **Уточнение приоритета (2026-09-25, владельцем):** при направлении Web-first сложная offline-синхронизация **не** является обязательным условием запуска первой web-версии; для Web допустимы локальные возможности браузера там, где это даёт практическую пользу; части [OFFLINE_SYNC.md](OFFLINE_SYNC.md) имеют статус Future/Under Design.

### ADR-008

- **ID:** ADR-008
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **Пользователь контролирует свою память, данные, команды и автоматизации** (просмотр, изменение, экспорт, удаление, управление сессиями/устройствами/интеграциями/памятью). **Приватность и безопасность закладываются с самого начала.** Доступ администрации к пользовательскому содержимому минимизирован (по умолчанию админ не видит приватные заметки и файлы). Направления: современный password hashing, безопасные сессии, CSRF/XSS/SQLi-защиты, authorization на каждом объекте, rate limiting, шифрование secrets, аудит, 2FA где нужно, backup + restore testing, export/delete account, least privilege. Чувствительные данные не хранятся «просто потому, что технически возможно»; платёжные секреты — без отдельной проработки безопасности не хранятся.
- **Причина:** Принципы явно заданы владельцем; Aven — центр личных данных.
- **Последствия:** Конкретные алгоритмы и механизмы — отдельные решения (Proposed/Under Discussion) вместе со стеком; интерфейс «Память Aven» обязателен; подробности — [SECURITY.md](SECURITY.md).

### ADR-009

- **ID:** ADR-009
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** Функциональность **расширяется модулями**; в будущем пользователь сможет создавать **собственные разделы/типы сущностей** (Аквариум, Мотоцикл, Домашнее животное, Сервер, Коллекция) с расширяемыми типами полей (text, long text, number, money, date, datetime, boolean, file, image, URL, select, relation, ...). Сущности связываются (пример: BMW → Ремонт → Расход → Чек → Автосервис → Дата); без дублирования одних данных в разных модулях без необходимости.
- **Причина:** Принцип явно задан владельцем в концепции.
- **Последствия:** Модель данных строится вокруг расширяемых сущностей и связей ([DATA_MODEL.md](DATA_MODEL.md)); конкретный механизм пользовательских типов (EAV/JSONB/гибрид) — **Under Discussion**; модули включаются/выключаются.

### ADR-010

- **ID:** ADR-010
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **Aven не должен сообщать об успешном выполнении невыполненного действия.** Различаются статусы: успешно; ожидает синхронизации; запланировано; требует подтверждения; ошибка; недоступно офлайн; платформа не поддерживает. Особенно важно для voice, notifications, integrations, desktop/mobile операций. Не обещать, что закрытый браузер сам произнесёт голосовое сообщение; Web Push и возможности браузера/ОС имеют ограничения.
- **Причина:** Принцип явно задан владельцем; доверие к помощнику строится на честной обратной связи.
- **Последствия:** У операций — машиночитаемые честные статусы; UI и шаблонные ответы отражают реальность; возможности платформ декларируются явно.

### ADR-011

- **ID:** ADR-011
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** Пользователь должен иметь возможность менять **подходящие настройки не только через UI, но и текстовой/голосовой командой**. Команды преобразуются в **стандартные Actions** (примеры: `settings.voice.rate.set`, `settings.quiet_hours.set`, `settings.day_period.set`, `dictionary.alias.create`, `settings.voice.enabled.set`, `settings.confirmation_policy.set`). **Голосовая команда не обходит security requirements**; для чувствительных настроек — подтверждение и/или повторная аутентификация.
- **Причина:** Принцип явно задан владельцем; соответствует духу продукта («скажи Aven, что сделать»).
- **Последствия:** Полный набор `settings.*` actions и перечень «подходящих»/«чувствительных» настроек — отдельные решения ([COMMAND_ENGINE.md](COMMAND_ENGINE.md), [SECURITY.md](SECURITY.md)); подробности — [PROJECT_PLAN.md](PROJECT_PLAN.md), раздел «Управление настройками через Aven».

### ADR-012

- **ID:** ADR-012
- **Дата:** 2026-09-25
- **Статус:** Product Principle
- **Решение:** **Разделение пользовательских и административных возможностей:** `/settings` — настройки конкретного пользователя; `/admin` — управление системой Aven. **Администратор не должен автоматически получать возможность действовать от имени пользователя.** Если в будущем появится support impersonation, функция должна проектироваться отдельно, иметь строгие permissions, быть явно заметной, журналироваться, иметь причину входа и по возможности ограничивать доступ к чувствительным данным.
- **Причина:** Принцип явно задан владельцем; минимизация доступа администрации к пользовательскому содержимому (см. ADR-008).
- **Последствия:** Механика разделения прав и будущий impersonation-ADR — отдельные решения ([SECURITY.md](SECURITY.md), [ADMIN.md](ADMIN.md)).

### ADR-013

- **ID:** ADR-013
- **Дата:** 2026-09-25
- **Статус:** Product Direction — **может быть пересмотрено владельцем проекта** (не необратимое архитектурное решение)
- **Решение:** Текущий приоритет разработки — **WEB-FIRST**. Первая полноценная реализация Aven — **самостоятельный полезный веб-сайт**. Первую версию **не** проектировать как: оболочку будущего мобильного приложения; только голосового ассистента; только чат; пустой dashboard; демонстрацию технологий. Пользователь полноценно пользуется основными функциями сайта мышью, клавиатурой, обычными формами, навигацией. **Голос и текстовые команды — дополнительные способы управления уже существующими функциями.** Единый путь действия:

  ```
  UI / Forms
          \
  Text command → Action Core → Business Logic → Data
          /
  Voice → STT → Text command

  Result → UI
         → Text response
         → TTS (если включено)
  ```

  Отдельная бизнес-логика для UI, текста и голоса **не создаётся**. Ориентировочный порядок развития: **Stage 1** — полезный самостоятельный веб-сайт → **Stage 2** — Command Engine и управление функциями сайта текстовыми командами → **Stage 3** — голосовой ввод/ответы поверх Command Engine → **Stage 4** — расширение автоматизаций и Automation Canvas → **Stage 5** — после появления полезного и проверенного веб-продукта определить, какие функции переносить в мобильные → **Stage 6** — Android/iOS и другие клиенты (отдельное будущее направление). Порядок отражает **продуктовый приоритет**, а не обязательную строгую последовательность реализации. **Web-first ≠ Web-only:** архитектура должна позволять дальнейшее развитие в PWA, Android, iOS, desktop, integrations, но эти платформы не являются текущей целью разработки.
- **Причина:** Направление задано владельцем 2026-09-25: сначала полезный и проверенный веб-продукт, затем перенос на другие платформы по подтверждённой необходимости.
- **Последствия:** UI/Forms — первоклассный источник команд наравне с текстом/голосом через единый Action Core; ни одна базовая функция не зависит от микрофона/STT/TTS; mobile и desktop остаются в [FUTURE.md](FUTURE.md) (Stage 5–6); сложная offline-синхронизация не является условием web-запуска (см. ADR-007, [OFFLINE_SYNC.md](OFFLINE_SYNC.md)); backend/API и бизнес-логика проектируются так, чтобы позже их могли использовать другие доверенные клиенты, без искусственных препятствий.

---

## Предложенные решения (Proposed — требуют утверждения, реализация до `Accepted` запрещена)

> **2026-09-26.** Для ADR-101…113 подготовлены материалы для решения: сравнение альтернатив, лицензии,
> версии, риски, стоимость владения и план проверок — [STACK_RESEARCH.md](STACK_RESEARCH.md).
> Статусы при этом **остаются `Proposed`**: ни одно техническое решение не принято, реализация запрещена.
> Список конкретных вопросов владельцу — [STACK_RESEARCH.md](STACK_RESEARCH.md) раздел 13.

### ADR-014

- **ID:** ADR-014
- **Дата:** 2026-09-25
- **Статус:** Proposed (реализовано в визуальном прототипе как UX-эксперимент; **требует утверждения** для production)
- **Решение:** Визуальный персонаж Aven (вымышленные образы Female «Ава» / Male «Авен») трактуется как **опциональный Presentation Layer**: он отвечает только за оформление (аватар, имя, приветствия, демо-профиль тембра TTS) и **не влияет** на бизнес-логику, данные, Command Engine и доступность функций. При отключённом персонаже интерфейс идентичен (нейтральный логотип «A»). Голос (TTS/STT) — надстройка поверх текста (ADR-003), не условие работы.
- **Причина:** Персонаж и голос — продуктовое оформление, повышающее вовлечённость, но не должен создавать зависимость базовых функций (согласовано с ADR-002, ADR-003, ADR-010, ADR-013).
- **Последствия:** Реализация персонажа изолируется в отдельные модули (`character.js`, `character.css`, ассеты) с честными fallback; никакие действия/данные не читают образ как обязательное условие. Для production потребуется отдельное утверждение (ассеты, лицензирование, бренд). Детали — [CHARACTER.md](CHARACTER.md).

### ADR-015

- **ID:** ADR-015
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Визуальная цель 3D Aven — **бюст**: голова + полная шея + немного плеч + верхняя часть груди, нижний край кадра немного ниже ключиц (кадр «собеседника видеозвонка»); НЕ персонаж до пояса и НЕ полное тело. Последовательность: сначала **только Female**; Male — позже, в том же стиле, после утверждения Female владельцем. Текущий этап — **visual concepts** (оригинальные 2D-рендеры для утверждения внешности, `review/3d-character-concepts/`), которые НЕ являются 3D-моделями. Интеграция победителя в основной интерфейс запрещена до явного «Выбираю вариант X» владельца. Процедурная Three.js-голова (если существует/пересоздаётся) — только Developer/Test Model («Use Test 3D Model»), не внешность Female Aven.
- **Причина:** Владелец изменил визуальное направление (2026-09-25): нужен качественный premium/semi-realistic digital human в кадре видеозвонка; внешность утверждает владелец до инвестиций в 3D-pipeline; бюст достаточен для продукта и дешевле в realtime (web).
- **Последствия:** Кандидаты и чек-лист требований к будущей модели (GLB/GLTF; rig head/neck/eyes/jaw; blendshapes blinkLeft/blinkRight/jawOpen/smile/frown/browUp/browDown/mouthClose; visemes REST/A/E/I/O/U/M-B-P/F-V с mapping при другом стандарте; hair cards без тяжёлой simulation; realtime-бюджет: triangles/texture resolution/material count/GLB size; PBR-текстуры; поддержка существующих состояний idle/listening/thinking/speaking/success/important/waiting/error без переписывания event bus и viseme abstraction) — [CHARACTER.md](CHARACTER.md) раздел 6 и `review/3d-character-concepts/README.md`. Pipeline настоящей модели (custom 3D / character creator / commissioned / licensed base) выбирается после утверждения внешности; лицензионная проверка сторонних base-моделей обязательна. До `Accepted` production-разработка 3D не начинается. Предыдущий 3D-прототип (коммит `ddd1c31`) не был push и невосстановим — учтено в WORK_LOG (запись VII).

### ADR-101

- **ID:** ADR-101
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Backend — TypeScript + Node.js (целевая версия **Node.js 24 LTS**; переход на 26 LTS после
  2026-10-28 — плановый). TypeScript **6.x** на старте, 7.x (Go-компилятор, релиз 2026-07-08) — после пилота в CI.
  Фреймворк — **NestJS** или **Fastify** (выбор за владельцем).
- **Причина:** Единый язык с веб-клиентом и общими схемами валидации; типизация; зрелая экосистема;
  LTS-поддержка Node 24 до 2028-04-30. NestJS даёт готовые модули/DI/структуру, Fastify — меньше абстракции
  и валидацию по JSON Schema из коробки.
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §4.2):** NestJS 11.x (MIT) — структура
  «всё включено», но «стоимость абстракции» для небольших сервисов; Fastify 5.x (MIT) — структура проекта на нас;
  Hono 4.x (MIT) — очень лёгкий, кросс-runtime, но архитектурные решения полностью свои; Express 5 (MIT) —
  минимум структуры. Bun/Deno как основной runtime на Stage 1 не рекомендуются. Цифры throughput из сторонних
  статей **не измерялись** и для личного ассистента критерием не являются.
- **Что проверить перед `Accepted`:** spike S1 ([STACK_RESEARCH.md](STACK_RESEARCH.md) §12) — скелет API
  с миграцией, RBAC, аудитом и сессиями/2FA на выбранном фреймворке.
- **Последствия:** До утверждения код не пишется. Прежний открытый вопрос («обоснование framework против
  альтернативы «лёгкий собственный сервер»») **закрыт материалами**: сравнение подготовлено, выбор за владельцем.
  Независимо от фреймворка доменная логика живёт в backend-модулях, а не в веб-слое (ARCHITECTURE §4,
  развилка R1 — [STACK_RESEARCH.md](STACK_RESEARCH.md) §3).

### ADR-102

- **ID:** ADR-102
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Серверная БД — **PostgreSQL 17 или 18** (точная мажорная версия фиксируется на старте
  по доступности у выбранного хостинга; 18.x — текущая стабильная линия, PostgreSQL 14 — EOL ноябрь 2026).
- **Причина:** Реляционная модель соответствует структурированным данным, связям сущностей и целостности;
  JSONB покрывает гибкие пользовательские типы; транзакции нужны для Undo/аудита/подтверждения опасных действий
  ([ADR-005](DECISIONS.md)); `numeric` — для денег (SECURITY §5); зрелые бэкап/PITR.
- **Альтернативы:** MySQL/MariaDB (нет преимуществ под нашу модель), SQLite на сервере (не для многопользовательской
  нагрузки; остаётся кандидатом для локальной БД мобильных клиентов — [ADR-104](#adr-104)), managed PostgreSQL
  (вопрос хостинга, а не СУБД — [STACK_RESEARCH.md](STACK_RESEARCH.md) §3, развилка R2).
- **Что проверить перед `Accepted`:** spike S1/S2 — миграции, append-only аудит, постановка фоновой задачи
  в одной транзакции с бизнес-записью.
- **Последствия:** Схема проектируется в [DATA_MODEL.md](DATA_MODEL.md) после утверждения; миграции — часть
  репозитория и админки (ADMIN §18); бэкап и **проверенное восстановление** — часть деплоя (ADMIN §17,
  SECURITY §8); механизм защиты критического audit trail — отдельный открытый вопрос №14.

### ADR-103

- **ID:** ADR-103
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Веб-клиент — React + TypeScript. Конкретный вариант зависит от развилки **R1**
  ([STACK_RESEARCH.md](STACK_RESEARCH.md) §3): **(Б)** отдельный API + клиент на **Vite + TanStack Router/Query**
  (рекомендация) или **(А)** full-stack **Next.js 16** (LTS-линия, self-host, standalone output).
- **Причина:** Экосистема и типизация React; при варианте Б доменная логика остаётся в backend-модулях и
  один Action Core обслуживает UI, Stage 2 (текст), Stage 3 (голос), API и будущих клиентов Stage 5–6
  (ARCHITECTURE §4, [FUTURE.md](FUTURE.md)).
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §5.1):** React Router v7 (library mode —
  SPA без «налога» full-stack; framework mode — бывший Remix, SSR опционален), TanStack Start 1.0 (SSR +
  типобезопасная маршрутизация, экосистема моложе), Next.js 16.3.x (самая большая экосистема; self-host
  поддерживается, но глубже всего интегрирован с Vercel; в сентябре 2026 закрыты две критические уязвимости
  в 16.3.3/15.5.24 — обязательна дисциплина патчей).
- **SSR не является обязательным:** личный кабинет не требует SEO; требование плана — полноценная работа
  мышью и клавиатурой (PROJECT_PLAN §6.2), что достигается и в SPA. PWA/offline — будущая перспектива
  ([ADR-007](DECISIONS.md), OFFLINE_SYNC), не условие запуска.
- **UI-слой:** headless-компоненты (Radix UI / React Aria) + CSS из прототипа — доступность не доделывается
  потом ([STACK_RESEARCH.md](STACK_RESEARCH.md) §5.2).
- **Что проверить перед `Accepted`:** spike S4 — одна реальная форма из прототипа на живом API, работа
  мышью и клавиатурой, a11y-аудит.
- **Последствия:** При варианте Б — два деплой-артефакта и общие схемы валидации (см. [ADR-111](#adr-111)).
  При варианте А — бизнес-логика всё равно выносится в `packages/core`, server actions остаются тонким слоем.

### ADR-104

- **ID:** ADR-104
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Мобильные приложения (будущее) — Flutter/Dart как один из кандидатов; локальная БД — SQLite.
- **Причина:** Кроссплатформенность Android/iOS из одной кодовой базы; SQLite — проверенное локальное хранилище.
- **Последствия:** Решение принимается перед этапом мобильных приложений; альтернативы (Kotlin Multiplatform,
  React Native) должны быть рассмотрены в ADR. **Исследование стека 2026-09-26
  ([STACK_RESEARCH.md](STACK_RESEARCH.md)) этот ADR не покрывает** — Stage 5–6 вне текущего приоритета.

### ADR-105

- **ID:** ADR-105
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Desktop-agent (будущее) — Rust + Tauri как кандидат.
- **Причина:** Компактный бинарник, системный доступ для разрешённых действий, безопасность Rust.
- **Последствия:** Desktop-agent запускается только с явного согласия, контролируемые permissions; не входит в MVP.
  **Исследование стека 2026-09-26 ([STACK_RESEARCH.md](STACK_RESEARCH.md)) этот ADR не покрывает** — Stage 5–6
  вне текущего приоритета.

### ADR-108

- **ID:** ADR-108
- **Дата:** 2026-09-25
- **Статус:** Proposed
- **Решение:** Файлы — за абстракцией `StorageProvider` (put/get/delete/signed URL, версионирование ключей);
  **целевой контракт — S3-совместимый API**. Реализация на Stage 1: локальный том + бэкап; переход на
  S3-реализацию (managed или self-hosted) — смена конфигурации, а не кода приложения.
- **Причина:** Файлы (чеки, фото, документы, экспорт данных пользователя) — часть модели данных; абстракция
  позволяет менять провайдера и self-hosted варианты без переписывания приложения.
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §6.4):** **MinIO не выбирать** —
  публичный репозиторий архивирован (последний push 2026-04-24), лицензия AGPLv3; SeaweedFS (Apache-2.0,
  зрелый), RustFS (Apache-2.0, молодой), Garage (AGPL-3.0, лёгкий/гео), Ceph (LGPL, избыточен для одного
  приложения), managed S3/R2/GCS (меньше операций; данные у провайдера — вопрос приватности владельцу).
- **Что проверить перед `Accepted`:** spike S3 — деплой и **восстановление из бэкапа** (файлы + БД).
- **Последствия:** Интерфейс хранилища фиксируется после утверждения; провайдер, лимиты и квоты — отдельно
  ([ADMIN.md](ADMIN.md), Storage); бэкап файлов — обязательная часть деплоя (SECURITY §8).

---

### ADR-109

- **ID:** ADR-109
- **Дата:** 2026-09-26
- **Статус:** Proposed
- **Решение:** Доступ к данным и миграции — **Drizzle ORM + drizzle-kit** (Apache-2.0): схема на TypeScript,
  SQL-близкие запросы, миграции в репозитории.
- **Причина:** Нужны контроль над SQL (JSONB для пользовательских сущностей, агрегаты финансов, append-only
  аудит) и отсутствие шага кодогенерации; миграции — обязательная часть админки (ADMIN §18).
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §4.4):** Prisma 7 (Apache-2.0; свой DSL,
  кодогенерация, Prisma Studio, в 7.x убран Rust-движок) — если DX и GUI важнее контроля над SQL;
  Kysely + SQL-миграции (MIT) — максимум контроля, но типы таблиц и отношения собираем сами;
  TypeORM/MikroORM — не рекомендуются для нового проекта.
- **Что проверить перед `Accepted`:** spike S1 — миграция + отношения + JSONB + аудит на реальной схеме.
- **Последствия:** Общие типы данных — в `packages/shared`; миграции применяются в деплое с rollback-планом;
  схема данных проектируется в [DATA_MODEL.md](DATA_MODEL.md).

### ADR-110

- **ID:** ADR-110
- **Дата:** 2026-09-26
- **Статус:** Proposed
- **Решение:** Аутентификация, сессии и 2FA — **Better Auth** (MIT) как встроенная библиотека (email/пароль,
  сессии в БД, OAuth, плагин TOTP/MFA, rate limiting) + **свой RBAC** (ADMIN §3) + свой аудит.
- **Причина:** Требования SECURITY §1/§10: authorization на каждом объекте, управление сессиями, 2FA,
  отдельные admin-сессии, rate limits. Встроенная библиотека держит данные в нашей БД и не требует
  эксплуатировать отдельный сервис.
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §6.5):** Auth.js v6 (сильнее связан с
  Next.js; MFA — плагинами); Zitadel (Apache-2.0) или Keycloak (Red Hat) как отдельный IdP — оправданы только
  при нескольких приложениях/SSO; **собственная реализация не рекомендуется** (риск ошибок в криптографии,
  сессиях, reset-потоках и rate limiting).
- **Что проверить перед `Accepted`:** spike S1 — вход, сессии в БД, принудительное завершение сессии,
  TOTP, отдельные admin-сессии, повторная аутентификация для чувствительной настройки ([ADR-011](DECISIONS.md)).
- **Последствия:** Политика обязательности 2FA по ролям (SECURITY §11) и перечень чувствительных настроек —
  открытые вопросы продукта, решаются отдельно; секреты интеграций шифруются (SECURITY §6, INTEGRATIONS).

### ADR-111

- **ID:** ADR-111
- **Дата:** 2026-09-26
- **Статус:** Proposed
- **Решение:** Контракт API — **REST + OpenAPI**, валидация — общие **Zod**-схемы (MIT) в `packages/shared`:
  один набор схем используется сервером (вход Action Core), веб-клиентом (формы) и генерацией OpenAPI.
- **Причина:** API — один из источников команд наравне с UI/текстом/голосом/автоматизациями (ARCHITECTURE §3)
  и должен переиспользоваться клиентами Stage 5–6 (Flutter/Rust), которым tRPC не подходит.
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §4.3):** tRPC (максимальная типобезопасность
  без кодогенерации, но контракт только для TypeScript-клиентов); GraphQL (гибкие выборки, но authorization
  по объектам и версионирование тяжелее — оверкилл для нашего набора разделов).
- **Что проверить перед `Accepted`:** spike S5 — изменение контракта ломает сборку клиента; OpenAPI совпадает с кодом.
- **Последствия:** Версионирование API (v1) и идемпотентность операций — требование для будущей синхронизации
  (OFFLINE_SYNC); документация API генерируется, а не пишется вручную.

### ADR-112

- **ID:** ADR-112
- **Дата:** 2026-09-26
- **Статус:** Proposed
- **Решение:** Тестирование и качество: **Vitest** (unit/integration), **Testing Library** (компоненты и
  доступность), **Playwright** (e2e), интеграционные тесты против **реального PostgreSQL** (Testcontainers или
  docker-compose-профиль). Линтер/форматтер — ESLint + Prettier **или** Biome (выбор за владельцем).
- **Причина:** Требование полноценной работы мышью и клавиатурой (PROJECT_PLAN §6.2) проверяется e2e;
  честность статусов ([ADR-010](DECISIONS.md)) и authorization на объекте (SECURITY §1) — интеграционными
  тестами на реальной БД. Нагрузочные тесты — позже (k6).
- **Альтернативы:** Jest (медленнее и тяжелее в монорепо на Vite), Cypress вместо Playwright (меньше контроля
  над несколькими контекстами/вкладками).
- **Что проверить перед `Accepted`:** spike S4 + запуск e2e в CI на PR.
- **Последствия:** CI (GitHub Actions): typecheck → lint → unit/integration → e2e → build → deploy после merge
  в `main`; покрытие критичных путей (деньги, аудит, опасные действия) обязательно.

### ADR-113

- **ID:** ADR-113
- **Дата:** 2026-09-26
- **Статус:** Proposed
- **Решение:** Репозиторий и деплой: **монорепо на pnpm workspaces** (`apps/api`, `apps/web`, `packages/shared`,
  `packages/config`), упаковка — **Docker Compose** на VPS с reverse proxy (Caddy/Traefik, TLS Let's Encrypt),
  CI/CD — **GitHub Actions**, бэкап БД и файлов + **проверенное восстановление** как часть деплоя.
- **Причина:** Один разработчик/агент и одно приложение: Kubernetes и PaaS-специфика несоразмерны
  (операционные расходы), а self-host соответствует приватности ([ADR-008](DECISIONS.md)) и бюджету
  (~€10–20/мес, [STACK_RESEARCH.md](STACK_RESEARCH.md) §8).
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §3, развилка R2):** PaaS (Railway/Fly/Render) —
  меньше операций, но данные у третьей стороны и дороже на объёме; гибрид (VPS + managed PostgreSQL +
  managed object storage) — меньше риск потери данных; Kubernetes — не рекомендуется на Stage 1.
  Turborepo/Nx — опционально, если понадобится кэш сборки.
- **Что проверить перед `Accepted`:** spike S3 — `docker compose up` на чистой машине, TLS, восстановление из бэкапа.
- **Последствия:** Регион хостинга и провайдер — решение владельца (данные личные); инфраструктура как код;
  обновления и патчи безопасности — регулярная обязанность (раздел 10 [STACK_RESEARCH.md](STACK_RESEARCH.md));
  health endpoint и логи — ADMIN §15–16, без логирования секретов (SECURITY §7).

---

## Ранее `Under Discussion` — переведены в `Proposed` (2026-09-26)

> ADR в статусе `Under Discussion` на 2026-09-26 **нет**: **ADR-106** и **ADR-107** переведены в `Proposed`,
> потому что подготовлены сравнение альтернатив и обоснование ([STACK_RESEARCH.md](STACK_RESEARCH.md) §6.2–6.3).
> Перевод статуса — **не** принятие решения: утверждение, как и по остальным ADR, за владельцем.
> Раздел сохранён, чтобы история статусов не потерялась.

### ADR-106

- **ID:** ADR-106
- **Дата:** 2026-09-25 (материалы подготовлены 2026-09-26)
- **Статус:** Proposed (был Under Discussion: кандидаты не были определены)
- **Решение:** Background jobs — **pg-boss**: очередь задач в том же PostgreSQL (`SKIP LOCKED`), отложенные
  и cron-задачи, retry с backoff, singleton, dead-letter-queue. Отдельный брокер не вводится.
- **Причина:** Фоновые задачи Aven — напоминания и события (FEATURES §4), «утро/вечер» (FEATURES §3.7),
  уведомления и email (ADMIN §10–11), бэкапы, пересчёт агрегатов, импорты: единицы-десятки задач в минуту.
  Ключевое преимущество — задача ставится **в той же транзакции**, что и бизнес-запись (нет окна
  «данные записали, а задача потерялась»), что согласуется с [ADR-005](DECISIONS.md) и [ADR-010](DECISIONS.md).
- **Альтернативы (сравнение — [STACK_RESEARCH.md](STACK_RESEARCH.md) §6.2):** BullMQ (Redis/Valkey; приоритеты,
  rate limiting, flows, готовый UI Bull Board) — нужен ещё один сервис; Temporal и подобные workflow-движки —
  отдельная инфраструктура, несоразмерна Stage 1; node-cron/OS cron — нет retry/DLQ/видимости и дублирование
  при нескольких инстанциях.
- **Критерии перехода на BullMQ + Valkey** (чтобы решение не было «навсегда»): устойчиво > ~1000 задач/мин,
  или жёсткий distributed rate limiting внешних API, или потоки зависимых задач (fan-out DAG), или заметное
  влияние очереди на PostgreSQL. Переход оформляется новым ADR.
- **Что проверить перед `Accepted`:** spike S2 — отложенная задача переживает перезапуск, DLQ видна,
  повтор не дублирует эффект.
- **Последствия:** Очереди, retry и failed runs видны в админке (ADMIN §18, [ADR-113](#adr-113));
  готовых дашбордов у pg-boss мало — раздел админки делаем свой.

### ADR-107

- **ID:** ADR-107
- **Дата:** 2026-09-25 (обоснование подготовлено 2026-09-26)
- **Статус:** Proposed (был Under Discussion: «Redis или совместимое — только если обосновано»; обоснование подготовлено)
- **Решение:** Отдельный кэш-сервер (Redis/Valkey) на Stage 1 **не вводится**. Потребности закрываются иначе:
  очередь — PostgreSQL ([ADR-106](#adr-106)), сессии — в БД (SECURITY §1 требует управления сессиями),
  кэш справочников — in-process LRU + HTTP-кэширование, realtime-обновления — SSE/WebSocket из приложения.
- **Причина:** Каждый лишний сервис — деплой, обновления, мониторинг, бэкап и поверхность атаки; для одного
  приложения это неоправданно **до появления измеренной необходимости**. Требование «только если обосновано»
  выполнено: обоснован отказ, а не введение компонента.
- **Критерии введения** (любой из): измеренная нагрузка на кэш/очередь сверх возможностей PostgreSQL;
  жёсткий distributed rate limiting нескольких инстанций; pub/sub между несколькими сервисами;
  горизонтальное масштабирование приложения без sticky-сессий.
- **Альтернативы и лицензии ([STACK_RESEARCH.md](STACK_RESEARCH.md) §6.3):** если компонент понадобится —
  **Valkey** 9.x (BSD-3, Linux Foundation, wire-совместим с Redis, дефолт в AWS ElastiCache), а не Redis 8.x —
  тот с мая 2025 под **AGPLv3** (copyleft: обязательства при распространении модификаций, нужна юридическая оценка).
- **Что проверить перед `Accepted`:** измерения в spike S2/S3 (нагрузка на БД, задержки) — вводить компонент без цифр нельзя.
- **Последствия:** Введение кэш-сервера оформляется новым ADR с результатами измерений; архитектура приложения
  не должна предполагать наличия внешнего кэша (иначе отказ от него станет скрытой зависимостью).
